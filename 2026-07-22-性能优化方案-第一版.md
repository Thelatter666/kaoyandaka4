# 砚台考研打卡 — 性能评估与优化方案

> 评估日期：2026-07-22
> 评估范围：client（React 18 + Vite 6）、server（Express 4 + MySQL 8）、shared、构建与部署链路
> 评估方法：源码静态审查 + 生产构建产物实测（`npx vite build`）+ chunk 内容取证分析

---

## 一、总体结论

项目整体工程素质较高：路由级代码分割、hover/idle 预取、极光背景降级（无 `filter: blur`、后台暂停、reduced-motion 适配）、rAF 重渲染收敛、DB 复合索引覆盖主要查询路径、gzip 压缩、登录限流等均已到位。

但审查发现 **2 个构建层关键缺陷导致首屏 JS 被无谓放大约 2.4 倍**，以及若干后端写放大、无事务、轮询策略问题。按优先级整理为 P0（4 项）/ P1（7 项）/ P2（6 项），预计 P0 完成后：

- 首屏（应用页）JS 下载量 **244 KB → 约 112 KB**（gzip 后 80 KB → 44 KB，**-45%**）
- 番茄钟页挂载的无效重查询消除，后台标签页轮询写库归零
- 介绍页图片流量 **1.2 MB → 约 200 KB**（-83%）

---

## 二、构建产物实测基线（2026-07-22，`vite build`）

| Chunk | 体积 | gzip | 加载时机（现状） |
|---|---|---|---|
| react-vendor | 134.67 KB | 43.23 KB | **首屏预加载** |
| motion-vendor (framer-motion) | 82.59 KB | 29.58 KB | **首屏预加载（异常，应仅介绍页按需）** |
| lucide-vendor | 27.14 KB | 7.22 KB | 首屏预加载 |
| PresetCard（共享 chunk） | 57.44 KB | 13.67 KB | 番茄钟/预设页（**内含 zod 运行时，异常**） |
| index（入口） | 16.70 KB | 6.37 KB | 首屏 |
| 各页面 chunk | 2–15 KB | — | 懒加载，正常 |
| CSS 合计 | ~90 KB | ~20 KB | 按需拆分，正常 |
| dist 总量 | 1.9 MB | — | 其中截图 PNG 占 1.2 MB |

---

## 三、P0 — 关键问题（建议立即修复）

### P0-1 manualChunks 对象语法缺陷：framer-motion 被全站首屏预加载

**证据链**（实测产物取证）：

1. [vite.config.ts](client/vite.config.ts) 使用对象形式：`{ 'react-vendor': ['react', 'react-dom'], ... }`。该形式只匹配**包入口模块**，`react/jsx-runtime`、`scheduler` 等子模块未被指派，被 Rollup 随机归入其他 vendor chunk。
2. 产物取证：`react-vendor` 首行为 `import{r}from"./lucide-vendor…"`（react 核心落入 lucide-vendor）；`PresetCard` chunk 首行 `import{j as g}from"./motion-vendor…"`（**jsx-runtime 落入 motion-vendor**）。
3. 后果：几乎所有 TSX 模块都依赖 jsx-runtime → 所有 chunk 静态依赖 motion-vendor → `dist/index.html` 对三个 vendor 全部 `<link rel="modulepreload">`。**framer-motion 82.59 KB（gzip 29.58 KB）在每个页面首屏都被下载执行，而全站仅介绍页（12 个 landing 组件）使用它。**

**方案**：改为函数形式，按 `node_modules` 路径完整归类：

```ts
manualChunks(id) {
  if (!id.includes('node_modules')) return;
  if (/[\\/]node_modules[\\/](react|react-dom|scheduler)[\\/]/.test(id)) return 'react-vendor';
  if (id.includes('/lucide-react/')) return 'lucide-vendor';
  if (/[\\/]node_modules[\\/](framer-motion|motion-dom|motion-utils)[\\/]/.test(id)) return 'motion-vendor';
}
```

**预期收益**：jsx-runtime 回归 react-vendor；motion-vendor 不再被入口静态依赖，仅在介绍页 chunk 加载时按需下载。应用页首屏 JS 244 KB → 161 KB（gzip 80 → 50 KB）。

**验证**：构建后检查 `dist/index.html`，确认 modulepreload 仅含 react-vendor / lucide-vendor；`grep -l "jsx-runtime" dist/assets/*.js` 确认归属 react-vendor。

---

### P0-2 zod 运行时泄漏进前端 bundle（约 50 KB）

**证据链**：

1. [shared/src/schemas/common.ts](shared/src/schemas/common.ts) 顶层同时定义 zod schema（`SubjectEnum = z.enum(...)`）与纯常量（`SHORT_BREAK_MINUTES`、`DURATION_QUICK_OPTIONS` 等）。
2. 客户端两处**值导入**常量：[PomodoroPage.tsx](client/src/pages/PomodoroPage.tsx)（`SHORT_BREAK_MINUTES` 等）、[DurationSelector.tsx](client/src/components/presets/DurationSelector.tsx)（`DURATION_QUICK_OPTIONS` 等）。
3. `z.enum()` 是顶层函数调用，Rollup 无法证明其纯净 → 整棵 zod 树保留。产物取证：PresetCard chunk（57.44 KB）开头即为 zod 内部实现（`arrayToEnum`/`invalid_type` 等），**该 chunk 约 85% 是 zod**。

**方案**：新建零依赖的 [shared/src/constants.ts](shared/src/constants.ts)，将纯常量（`SHORT_BREAK_MINUTES`、`LONG_BREAK_MINUTES`、`LONG_BREAK_AFTER_ROUNDS`、`DURATION_QUICK_OPTIONS`、`DURATION_STEP`、`DURATION_MIN`、`DURATION_MAX`、`EXAM_DATE`）迁入；`schemas/common.ts` 改为 `export ... from '../constants'` 保持服务端引用兼容；客户端两处导入改指向 constants。

**预期收益**：番茄钟/预设路径 chunk 57 KB → 约 6 KB（-51 KB，gzip -12 KB）；前端 bundle 完全不含 zod。

**验证**：`grep -c "invalid_type" dist/assets/*.js` 应为 0。

---

### P0-3 useFocusSession 每 10 秒无差别轮询

**现状**：[useFocusSession.ts](client/src/hooks/useFocusSession.ts) `setInterval(checkActive, 10000)`，番茄钟页挂载期间永久轮询 `/focus/active`，**页面切后台也继续**（浏览器仅降频不停止），且每次请求伴随会话 `rolling: true` 的 sessions 表写操作（见 P1-4），形成持续读+写负载。

**方案**：

1. `document.hidden` 时 `clearInterval`，`visibilitychange` 恢复时立即拉取一次后重启轮询（与极光背景 `page-hidden` 机制同构）。
2. 无活跃会话且非番茄钟上下文时可将间隔放宽至 30–60 s；有活跃会话时保持 10 s。
3. 长期可演进为 SSE/事件驱动，当前阶段轮询+可见性暂停性价比最高。

**预期收益**：后台标签页请求与 sessions 写库归零；空闲期请求量下降 50–80%。

---

### P0-4 番茄钟页挂载即调 statistics/forest(day) 仅为取一个计数

**现状**：[PomodoroPage.tsx](client/src/pages/PomodoroPage.tsx) 挂载时调用 `statisticsApi.getForest('day', today())` 只为拿 `totalCompletedSessions`。而 [statistics.ts](server/src/routes/statistics.ts) 该接口执行 **3 条 SQL**：范围明细 `SELECT *`（含全部记录行）+ 2 条全量累计聚合，响应体含完整记录列表，前端全部丢弃。

**方案**：新增轻量端点 `GET /api/v1/statistics/today-summary`，SQL 内聚合：

```sql
SELECT COUNT(*) AS completed_sessions, COALESCE(SUM(actual_duration_seconds),0) AS total_seconds
FROM study_records
WHERE user_id = ? AND created_at >= ? AND created_at < ?
  AND (source = 'focus_session' OR (source = 'course_video' AND focus_session_id IS NULL));
```

**预期收益**：1 条聚合 SQL 替换 3 条（含一次全行传输）；响应体从 KB 级降到百字节。

---

## 四、P1 — 高价值优化

### P1-1 介绍页截图 1.2 MB → WebP/AVIF

[client/public/screenshots/](client/public/screenshots/) 三张 PNG：384 KB + 498 KB + 346 KB。虽 `loading="lazy"`，仍是介绍页主要流量。

- 转 WebP（或 AVIF + WebP 回退，`<picture>`），质量 80，预计合计 ≤ 200 KB（-83%）。
- `<img>` 补 `width`/`height` 属性消除 CLS；补充 `srcset` 适配高 DPR。
- 截图脚本 [take-landing-screenshots.mjs](e2e/take-landing-screenshots.mjs) 产物后接压缩步骤（`sharp`）。

### P1-2 多写操作缺事务（原子性 + 往返次数）

以下端点存在「先写 A 再写 B」的多语句序列，中途失败留下脏数据，且每条语句一次独立网络往返：

- [focus.ts](server/src/routes/focus.ts) `POST /:id/complete` 与 `/active` 自动完成：`UPDATE focus_sessions` + `INSERT study_records`
- [courses.ts](server/src/routes/courses.ts) `POST /`：课程 INSERT + N 条集数 INSERT；`PATCH .../toggle`：UPDATE + INSERT/DELETE study_records

**方案**：`pool.getConnection()` → `BEGIN/COMMIT/ROLLBACK` 包裹多写序列。

### P1-3 循环单条写 → 批量写

- [courses.ts](server/src/routes/courses.ts) 导入课程时逐条 `INSERT` 集数（N 次往返）→ 单语句多 `VALUES` 批量 INSERT（一门 100 集课程：100 次往返 → 1 次）。
- [tasks.ts](server/src/routes/tasks.ts) `PATCH /reorder` 循环逐条 UPDATE → 事务包裹，或 `INSERT ... ON DUPLICATE KEY UPDATE` 批量。

### P1-4 会话 rolling:true 每请求写库

[index.ts](server/src/index.ts) `rolling: true` 使**每个 API 请求**都 UPDATE sessions 表续期。叠加 10 s 轮询，空闲期也存在恒定写负载。

- 建议 `rolling: false`（7 天固定会话，到期重新登录），或自定义中间件仅在会话剩余不足一半时 touch 一次。
- 配套 P0-3 的轮询暂停，收益叠加。

### P1-5 生产环境缺失 trust proxy（部署阻断级）

按部署方案（nginx 反代 + PM2），Express 位于 nginx 之后：

- 未设 `app.set('trust proxy', 1)` 时，express-rate-limit v8 检测到 `X-Forwarded-For` 头会抛 `ValidationError`；且所有请求源 IP 均为 127.0.0.1，登录限流退化为**全局限流**（一个用户触发，所有用户被拒）。
- 同时 `req.protocol` 误判会影响 secure cookie 逻辑。

**方案**：[index.ts](server/src/index.ts) 增加 `app.set('trust proxy', 1)`（仅信任第一跳 nginx）。

### P1-6 LazyMotion 静态导入 domAnimation，按需加载失效

[LandingPage.tsx](client/src/components/landing/LandingPage.tsx) `import { LazyMotion, domAnimation } from 'framer-motion'` 静态引入特性包，`LazyMotion` 的按需意义被抵消。改为：

```tsx
<LazyMotion features={() => import('framer-motion').then((m) => m.domAnimation)} strict>
```

配合 P0-1 修复后，介绍页动效运行时才真正按需。

### P1-7 SmoothRing 60 fps setState → ref 直写 DOM

[PomodoroPage.tsx](client/src/pages/PomodoroPage.tsx) `SmoothRing` 每帧 `setNowMs` 触发 React 重渲染（28 个 SVG circle 的 reconcile）。虽已 `React.memo` 收敛不影响页面其他部分，但主线程每帧仍有 JS 开销。

**方案**：rAF 回调内用 ref 直接写进度环 `stroke-dashoffset` 属性；`Math.ceil` 后的整数秒变化时才 setState 更新中心文字。预期主线程 JS 占用下降一个数量级，视觉完全一致。

---

## 五、P2 — 常规优化（按计划排期）

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| P2-1 | 统计页时间线无渲染上限，月模式可达数百个 Card | [StatisticsPage.tsx](client/src/pages/StatisticsPage.tsx) | 日分组默认折叠仅展开最近 3 天，或引入分页/虚拟滚动 |
| P2-2 | 课程列表 5 个相关子查询/行 | [courses.ts](server/src/routes/courses.ts) `COURSE_QUERY` | 单条 `LEFT JOIN course_episodes + GROUP BY` 聚合（数据量小，优先级低） |
| P2-3 | 累计统计每次全量扫描用户全部记录 | [statistics.ts](server/src/routes/statistics.ts) | 数据量随年份增长后，引入 `user_stats` 汇总表（完成会话时同步累加）或进程内短 TTL 缓存 |
| P2-4 | 仓库无 nginx 配置，静态资源缓存/压缩策略缺失 | 部署层 | 见附录参考配置：`assets/` 指纹文件 `immutable` 长缓存、`index.html` `no-cache`、brotli/gzip、API 反代与 `X-Forwarded-For` 透传 |
| P2-5 | e2e/tests 目录为空，`test:e2e` 无测试可跑；缺性能预算守门 | [e2e/tests](e2e/tests) | 补最小冒烟集 + Lighthouse CI 或 Playwright 性能断言（首屏 JS ≤ 160 KB、LCP ≤ 2.5 s） |
| P2-6 | 页面切换重拉数据，无客户端缓存 | client 各页面 | 个人应用数据新鲜度优先，现状可接受；如需优化可引入 SWR/TanStack Query（体积成本需权衡） |

### 附带发现（功能 bug，非性能）

- [PlanPage.tsx](client/src/pages/PlanPage.tsx) `getYesterday()` 使用 `toISOString()`（UTC 日期），东八区每日 0–8 点会得到错误的「昨天」，导致昨日未完成任务顺延提醒失效。应改用本地日期格式化（与 `utils/date.ts` 的 `formatDate` 一致）。
- [StatisticsPage.tsx](client/src/pages/StatisticsPage.tsx) `import { ForestResponse }` 未使用 `import type`，建议统一以符合 `isolatedModules` 习惯。

---

## 六、当前已做好的性能实践（保持）

1. 路由级代码分割 + hover/focus/idle 三档 chunk 预取，页面切换接近零等待。
2. 极光背景：`radial-gradient` 软边替代 `filter: blur`、`will-change: transform`、后台标签页暂停漂移、reduced-motion 静止。
3. rAF 重渲染收敛于 `SmoothRing`/`MiniSessionRing` 内部，页面级组件树不随帧/秒抖动。
4. `backdrop-filter` 有 `@supports` 不透明降级；骨架屏 shimmer 仅首载播放，范围切换不重播动画。
5. 服务端：gzip 压缩、连接池（10）、`dateStrings` 免时区转换、登录限流 + bcrypt 占位哈希时序对齐、会话固定防护、业务路由集中鉴权。
6. DB：tasks `(user_id, task_date, is_important, sort_order)` 等复合索引覆盖主查询路径；episodes 冗余 `user_id` 避免 JOIN。
7. 构建 1.36 s，产物无 sourcemap 泄漏；CSS 按需拆分。

---

## 七、实施路线图

| 阶段 | 内容 | 预估工作量 | 验收标准 |
|---|---|---|---|
| 第 1 阶段（构建层） | P0-1 manualChunks 函数化、P0-2 常量抽离、P1-6 LazyMotion 动态特性 | 0.5 天 | `dist/index.html` 无 motion modulepreload；bundle 无 zod；应用页首屏 JS ≤ 165 KB（gzip ≤ 52 KB） |
| 第 2 阶段（运行层） | P0-3 轮询可见性暂停、P0-4 today-summary 端点、P1-7 ref 直写圆环 | 1 天 | 后台标签页零请求；番茄钟挂载 1 条聚合 SQL；rAF 期间无 React 提交 |
| 第 3 阶段（服务端） | P1-2 事务、P1-3 批量写、P1-4 rolling 调整、P1-5 trust proxy | 0.5 天 | 多写端点单事务；导入 100 集 1 条 INSERT；nginx 后限流按真实 IP |
| 第 4 阶段（资源与守门） | P1-1 截图 WebP、P2-4 nginx 配置入库、P2-5 e2e 冒烟 + 性能预算 | 1 天 | 截图总量 ≤ 250 KB；CI 性能预算断言通过 |

---

## 八、性能预算（建议纳入 CI 守门）

| 指标 | 预算 | 测量方式 |
|---|---|---|
| 应用页首屏 JS（gzip） | ≤ 52 KB | `vite build` 产物 + index.html modulepreload 审计 |
| 介绍页首屏 JS（gzip，含 motion） | ≤ 82 KB | 同上 |
| 任意 chunk 含 zod | 0 | `grep invalid_type dist/assets/*.js` |
| 介绍页图片总量 | ≤ 250 KB | `du -sh client/public/screenshots` |
| LCP（介绍页，本地 4G 节流） | ≤ 2.5 s | Playwright + Lighthouse |
| 后台标签页 API 请求 | 0 次/分钟 | Playwright `page.emulateMedia` + 网络监听 |

---

## 九、附录：nginx 参考配置（当前仓库缺失，建议入库 `deploy/nginx.conf`）

```nginx
server {
  listen 443 ssl http2;
  server_name your-domain.com;

  root /var/www/kaoyandaily/client/dist;
  index index.html;

  # 指纹静态资源：长缓存 + immutable
  location /assets/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
  }

  # 截图等 public 资源：协商缓存
  location /screenshots/ {
    expires 7d;
    add_header Cache-Control "public";
  }

  # HTML：不缓存，保证发版即生效
  location / {
    add_header Cache-Control "no-cache";
    try_files $uri $uri/ /index.html;
  }

  # API 反代：透传真实 IP（配合 app.set('trust proxy', 1)）
  location /api/ {
    proxy_pass http://127.0.0.1:3001;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # brotli（如模块可用）+ gzip 兜底
  gzip on;
  gzip_types text/css application/javascript application/json image/svg+xml;
  gzip_min_length 1k;
}
```

---

## 十、第二轮补充审查（2026-07-22，施工前复核）

复核结论：第三节至第五节全部 P0/P1/P2 问题在当前代码中**逐条核验仍存在**，方案前提成立。另发现以下补充项（已并入实施路线图对应阶段）：

### 补-1（P1，并入第 2 阶段）useFocusSession 绕过统一 api client

[useFocusSession.ts](client/src/hooks/useFocusSession.ts) `checkActive` 使用原生 `fetch('/api/v1/focus/active')`，绕过 [client.ts](client/src/api/client.ts)：401（会话失效）不会触发全局登出回调；轮询失败时每 10 秒重复 `setError` 触发无意义重渲染。改用 `focusApi.getActive()`，失败静默保留上次状态。

### 补-2（P2，并入第 3 阶段）compression 无阈值配置

[index.ts](server/src/index.ts) `compression()` 零配置，对百字节小响应也压缩，CPU 得不偿失。改为 `compression({ threshold: 1024 })`。

### 补-3（P2，配合 P0-4/P2-3）statistics /forest SELECT * 字段瘦身

[statistics.ts](server/src/routes/statistics.ts) 范围明细 `SELECT *` 拖出 `notes` TEXT、`updated_at` 等前端不用的字段。明确列出所需 7 列，降低行传输体积。

### 补-4（P2，暂不实施，记录备查）api client 无请求取消与去重

[client.ts](client/src/api/client.ts) 无 AbortController：快速切换路由时旧请求无法取消，理论上可能晚到覆盖新状态。当前页面均有 `cancelled` 标志或组件卸载保护，实际风险低；如后续引入数据缓存层（SWR/TanStack Query，见 P2-6）一并解决。

### 补-5（说明）根 package.json 的 zod 依赖位置

zod 位于根 `dependencies` 供 shared 解析，server 另有自身依赖。位置本身不决定是否进前端产物（由 P0-2 的导入切断决定），不做迁移；以 `grep invalid_type dist/assets/*.js` 为最终验收。

### 复核中排除的误报

- `online_courses` 的 `WHERE id = ? AND user_id = ?`：id 为主键，主键定位后过滤，无需复合索引。
- HomePage 三个请求已是并行发起（非串行 await），无需改动。

---

*本方案基于 2026-07-22 代码快照审查，问题定位均附产物级证据；P0 项改动小、收益确定，建议优先落地。*
