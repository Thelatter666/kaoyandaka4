# 全库健康检查报告(2026-08-30)

> 检查对象:`main` @ `691e42f`(2026-08-29 00:50,工作区干净)
> 环境:macOS arm64 / node v24.14.0 / npm 11.18.0,本地 MySQL 在线
> 方式:只读体检 —— 类型/lint/单测/构建/E2E/依赖审计/安全扫描/质量指标/文档漂移核对,**除本报告外未改动任何文件,未提交**

## 总评

**工程过程非常健康(全链路绿),但存在 1 个必须立即处置的 P0 安全事故:生产密钥随 `deploy/.env.production` 提交进了公开的 GitHub/Gitee 仓库。**

| 维度 | 结论 | 说明 |
|---|---|---|
| 类型检查 | 🟢 | client / server `tsc --noEmit` 均 0 错误 |
| 生产构建 | 🟢 | client 6.03s + server tsc 全过 |
| 单元测试 | 🟢 | **15 文件 / 126 tests 全绿**(1.6s),与 AGENT.md 基线一致 |
| E2E | 🟢 | smoke 1 passed(3.7s);仅 1 个用例为已知覆盖缺口 |
| ESLint | 🟡 | 1 error + 8 warnings(与 AGENT.md 记录逐字一致,无新增) |
| 依赖安全 | 🟡 | server 侧 1 个 **high** 漏洞(可 `npm audit fix` 自动修);其余 3 包 0 |
| 性能预算 | 🔴 | 首屏 JS **367.6KB / 预算 200KB,超 84%** |
| 信息安全 | 🔴 | **P0:生产密钥已公开泄露**(见下) |
| 代码质量 | 🟢 | 0 TODO / 0 `as any` / 0 ts-ignore;两处 900+ 行大文件为长期项 |
| 文档同步 | 🟢 | 绝大多数声称与代码实测一致;2 处漂移(见「文档漂移」) |
| Git 卫生 | 🟢 | 工作区干净、322 个跟踪文件、.gitignore 覆盖良好(唯 P0 例外) |

---

## P0:生产密钥公开泄露(需立即处置)

**证据(已脱敏)**:`deploy/.env.production` 被 git 跟踪并已推送双远端,内含全部生产配置:

- `SESSION_SECRET`:已设置,**64 位真实密钥**
- `DB_PASSWORD`:已设置,**仅 4 位**(即使不泄露也是弱密码)
- `DB_USER` / `DB_NAME` / `DB_PORT` 等基础设施信息

**暴露面实测**:GitHub 仓库 `Thelatter666/kaoyandaka4` 为 **PUBLIC**(`gh repo view` 确认);Gitee `thelatter123/kaoyandaka4` HTTP 200 可匿名访问。另有两项次级暴露:服务器公网 IP `118.24.164.3` 出现在 3 个跟踪文件(`deploy/nginx.ip.conf`、`deploy/deploy.sh`、`deploy/server-management-prompt.txt`,后者还含宝塔面板路径 `/www/server/panel`)。

**为什么严重**:SESSION_SECRET 可伪造会话 cookie(签名的完整性依赖它的保密);DB 口令 + 公网 IP + 3306 潜在暴露面组合攻击成本极低。密钥一旦进过公开远端历史,**轮换是唯一充分措施**(撤文件不够,fork/缓存无法追回)。

**处置步骤(按序,涉及破坏性操作,执行前建议备份)**:

1. **立即轮换**(服务器上):重新生成 64 位随机 `SESSION_SECRET`;MySQL 用户改强密码;`user_settings` 会话随 secret 轮换全部失效,属预期。
2. 从工作区移除跟踪:`git rm --cached deploy/.env.production`,在 `.gitignore` 增加 `deploy/.env.production`(建议在 `deploy/` 留一份 `.env.production.example` 模板,只含键名)。
3. **历史清洗**:`git filter-repo --path deploy/.env.production --invert-paths`,然后双远端 force push(`git push github main --force` + `git push origin main --force`)。注意 force push 属破坏性操作。
4. 评估 IP/宝塔路径暴露:nginx.conf 与 deploy.sh 中的 IP 如需收敛,可改为占位符 + 本地注入。
5. 完成后按 AGENT.md 流程单独提交 `fix(security):` 或 `chore(security):`。

---

## 性能预算:首屏超支 84%(P1,根因已定位)

`node e2e/check-perf-budget.mjs` **未通过**(脚本退出码 1,行为正确):

```
✗ 首屏 JS 合计 367.6KB(预算 200.0KB)
    index-CWCnYZeS.js: 63.1KB
    react-vendor:     139.6KB
    lucide-vendor:     25.0KB
    motion-vendor:    139.9KB   ← 异常:动效库进了首屏
✗ index.html modulepreload 含 motion-vendor
✓ screenshots 73.9KB(预算 250KB)
```

**根因**:`client/src/components/layout/TopNav.tsx:12` 静态 `import { motion, useReducedMotion } from 'framer-motion'`,而 TopNav 被 `App.tsx` 静态引入 → motion-vendor(143KB/gzip 48KB)进入入口依赖图并被 modulepreload。页面级 `React.lazy` 分割做得很好,但顶栏这条静态链绕过了它。

**建议方向**(任选其一评估):TopNav 内使用 motion 的子组件(如主题切换/下拉动效)拆为 lazy;或顶栏动效降级为 CSS transition(库内已有 token 体系);目标把 motion-vendor 移出入口图。注:预算脚本按原始字节数计,gzip 后实际传输约 120KB,但既然定了 200KB 预算就应回归预算,或明确修订预算口径。

---

## 依赖

- **1 个 high 漏洞(可自动修)**:`server` 生产链 `express-rate-limit@8.6.0 → ip-address@10.2.0`(≤10.3.0,三个 SSRF/分类绕过 CVE)。`npm audit` 显示 `fix available`,在 `server/` 执行 `npm audit fix` 即可(补丁版本内)。
- root / client / e2e 生产依赖 **0 漏洞**。
- **过期主版本**(均为 dev 侧或大版本跨越,暂不阻塞):framer-motion 11→13、@vitejs/plugin-react 4→6、@types/react 18→19。React 18.3 本体不在过期列表。
- **双图标库**:`lucide-react` 37 个文件在用,`@tabler/icons-react` 仅 `FileUpload.tsx` 1 处 —— 建议统一为 lucide 后移除 tabler 依赖。
- 快速未用依赖扫描:**未发现未使用依赖**(react-day-picker/date-fns/@tanstack/react-virtual/uuid/bcrypt/compression 等均有真实引用)。
- `bcrypt@6` 原生编译需 install script 白名单(npm allow-scripts 提示),构建机需注意。

---

## 代码质量

- 规模:源码 **17,131 行** + 测试 1,627 行(单测占比 ~9.5%,对个人项目尚可)。
- 大文件 TOP2:`client/src/pages/PomodoroPage.tsx` **917 行**、`client/src/local/localStore.ts` **910 行** —— 均为功能复杂度真实聚集处,建议作为下次重构候选(P2)。
- 优于常见水位:全库 **0** TODO/FIXME、**0** `as any`、**0** `@ts-ignore`,仅 1 处 `eslint-disable`(Modal.tsx:45)。
- `console.*` 共 43 处,分布良性:server 侧为启动路由清单 + errorHandler 兜底日志,client 侧集中在 `utils/sound.ts` 的音频降级日志 —— 无需处理。
- ESLint 唯一 error 仍是已知长期项:`Modal.tsx:45` 引用了未加载的 `react-hooks/exhaustive-deps` 规则(eslint.config.js 未装载 react-hooks 插件)。修法二选一:配置里补插件;或删掉该行 disable(规则本就未生效)。
- 8 个 warnings:5 个 unused-vars(AnimatedThemeToggle/Card3D/Magnetic)、2 个 `no-explicit-any`(ImportCourseModal:90、Card3D:102)、2 个 no-unused-expressions。

## 安全细项(除 P0 外,均为良好或可接受)

- ✅ SQL 注入:全库仅 1 处模板字符串拼接(`server/src/routes/import.ts:174` `DELETE FROM ${TABLE_DEFS[key].table}`),表名来自 `import-mapping.ts` 的**内部硬编码映射**,键为固定联合类型,非用户输入 —— 安全。其余查询全部参数化(`WHERE user_id = ?`)。
- ✅ 鉴权挂载:10/11 路由挂载层 `requireAuth`;`/import` 例外为 AGENT.md 记录在案的设计(handler 内解析会话并支持归属重指向),代码与文档一致。
- ✅ Zod 校验覆盖:11 个路由文件中 10 个用 `validate()`;`export.ts` 为无参数 GET,可接受。
- ✅ `SESSION_SECRET` 缺失拒绝启动(无弱默认);会话 7 天不滚动;限流器就位(login/register/import/reviewLock-verify)。
- ✅ 本地模式复盘锁用 SHA-256+salt 属 ADR-0005 记录在案的设计(服务器侧为 bcrypt)。
- ✅ `.env`、`.claude/`、`*.png`、`.DS_Store` 等均正确忽略;`server-butler/` 密钥走 `~/.tccli` 注入不在库内。

## E2E 与测试覆盖

- smoke 用例质量不错(标题/Hero/console error/webp 断言 + 401 噪声过滤),本次实跑通过。
- 全部 E2E 仅 1 用例(AGENT.md 已声明),核心流程(登录→打卡→专注→复盘)无端到端覆盖 —— 长期缺口,维持文档既有结论。

## 文档漂移核对(AGENT.md / ARCHITECTURE.md 声称 vs 实测)

实测一致 ✅:11 个路由文件与端点清单、12 个 lazy 页面、ui/ 24 组件清单、7 个 hooks、plans 总账(18 条 = DONE 12 / TODO 6)、单测基线(15 文件/126 tests)、ADR 0001-0006 存在、双白名单路由(`App.tsx:54` PUBLIC_PAGES)、manualChunks 4 vendor、`.env` 从项目根加载。

发现 2 处漂移(P2,建议随下次 `docs(agent):` 提交修正):

1. **ARCHITECTURE.md「已知缺口」称 `AuthPage.css` 为"疑似死文件,可清理" —— 事实错误**:它被 `LoginPage.tsx`、`RegisterPage.tsx`、`LocalModePage.tsx` 三处实际 import,不可清理。
2. ARCHITECTURE.md 目录树中 `docs/adr/` 标注"砚池 4 条",实际已有 **6 条**(0005 复盘锁、0006 暂停)。

## 建议清单

| 优先级 | 事项 |
|---|---|
| **P0** | 轮换 SESSION_SECRET + DB 密码(密码顺带从 4 位换强)→ `git rm --cached` + .gitignore → filter-repo 历史清洗 + 双远端 force push |
| **P1** | `server/ npm audit fix`(ip-address high);首屏 motion-vendor 移出入口图回归 200KB 预算;eslint.config.js 装载 react-hooks 插件消掉 Modal.tsx error |
| **P2** | 修正 ARCHITECTURE.md 两处漂移(AuthPage.css 非死文件、ADR 6 条);FileUpload 统一到 lucide 并移除 @tabler;8 个 lint warnings 清理;PomodoroPage/localStore 拆分;E2E 补核心流程用例 |

## 本次执行记录(可复现)

```
npx tsc --noEmit (client/server)  → 双 0 错误
npx eslint .                      → 1 error + 8 warnings
npx vitest run                    → 15 files / 126 tests passed (1.63s)
npm run build                     → client 6.03s + server tsc ✅
npm run test:e2e                  → 1 passed (3.7s)
node e2e/check-perf-budget.mjs    → FAIL(退出码 1)
npm audit --omit=dev ×4 包        → 仅 server 1 high
node --version                    → v24.14.0
```
