# 面试稿 · 项目介绍（AI Coding 视角）

> 项目：砚台考研打卡（kaoyandaily）—— 个人考研学习管理网站
> 用途：面试时口头介绍本项目，主线是「我如何用 AI 工程化地做完一个真实上线的产品」
> 建议时长：完整版 3–4 分钟；文末有 30 秒电梯版

---

## 一、开场（30 秒，定基调）

我做的这个项目叫「砚台考研打卡」，是一个考研学习管理网站，2026 年 7 月启动，现在已经公网部署上线了。

但我想重点讲的不是这个网站本身，而是**它是我用 AI 编程完整跑通的一个真实工程案例**——不是拿 AI 补全几行代码，而是让 AI 从需求、设计、实现到验证、部署，全程参与，然后我用一套工程方法把这套流程管起来。

一句话概括我的收获：**AI 写代码的能力不是瓶颈，"怎么让它稳定、可验证、不跑偏"才是。这个项目就是我的答案。**

---

## 二、项目本身（40 秒，先给事实）

- **是什么**：考研学习管理，番茄钟专注计时、每日任务打卡、网课进度、学习森林统计、每日复盘、数据备份迁移。目标用户就是我本人，考试日期 2026-12-20。
- **技术栈**：React 18 + TypeScript + Vite 前端，Express 4 + TypeScript 后端，MySQL 8，Zod 前后端共享校验，测试用 Vitest + Playwright。
- **规模**：约 200 个源码文件、1.8 万行 TS/TSX，11 个后端路由、9 张表、12 个懒加载页面、24 个 UI 组件，196 次提交。
- **质量基线**：126 个单元测试全绿，tsc 零错误，ESLint 零 error 零 warning，首屏 JS 控制在 200KB 预算内。
- **上线状态**：腾讯云 + nginx + PM2 常驻，GitHub / Gitee 双远端，已完成域名与备案。

这里有个细节可以体现 AI 参与的深度：**这 1.8 万行代码里，绝大多数是 AI 写的；但架构决策、验收标准、红线规则，是我定的。**

---

## 三、核心：我怎么让 AI 干得又快又稳（90 秒，这是回答主线）

我把 AI 编程当成"带一个能力很强但会失忆、会过度自信、会自作主张的工程师"。针对这三点，我做了三件事。

### 1. 给 AI 建"长期记忆"——用文档做上下文工程

AI 每次开新对话都会失忆，所以我不能靠聊天记录。我把项目知识固化成几个各司其职的文件：

- **`AGENT.md`**：项目的"驾驶手册"。原则很明确——**只写读代码读不出来的东西**：不可逆的决策、踩过的坑、安全红线、开发工作流。比如"某些接口的鉴权必须下沉到 handler 里、不能挂在挂载层"、"颜色必须用 CSS 变量不允许硬编码"。
- **`ARCHITECTURE.md`**：枚举型事实，目录结构、组件清单、端点清单，让 AI 不用每次翻代码。
- **`CONTEXT.md`**：领域术语表。这个很关键——我把「专注会话」「轮」「砚池」这些概念先定义清楚，AI 才不会把"暂停"和"休息"混为一谈。
- **`memory.md`**：事故教训。每次踩坑都写死在这里，变成下一次的红线。

而且这些文档有**维护规则**：只要改了文档里记录的事实，必须同步更新文档。这样上下文不会腐烂。

### 2. 给 AI 定"工作流"——人工卡在关键节点

我给 AI 定了强制流程：提需求 → 探索理解 → **复述对齐（我确认后才动手）** → 建分支 → 执行（先出设计文档、再出实施计划、最后写代码）→ **效果确认（它只许报效果，由我验收）** → **我明确下令才允许提交**。

核心是三个"人类决策点"必须停下来问我，其余它自己查、自己判断。这样既不让它乱来，也不让它变成每一步都要问的机器人。

### 3. 给 AI 配"执行单元"——子代理 + 任务书

大任务我不让它一口气写，而是拆成一个个自包含的小任务，一个任务派一个子代理。任务书里必须写清五样东西：目标、必读文件清单、可修改范围、项目硬性约束、验收标准。

有个规则我觉得很重要：**子代理返回后我必须亲自验收，不听它自己汇报"已完成"。** 我跑 lint、跑构建、跑测试，不通过就带错误重新派。这条规则拦住过不少"看起来对"的实现。

---

## 四、三个具体案例（60 秒，用故事证明不是空谈）

**案例一：用测试驱动交付一个功能。**
做"复盘锁"（进入复盘页要二次密码）和"专注暂停"时，我让 AI 先出设计文档、写下 6 条架构决策记录，再拆成 14 个任务、分 3 批、每个任务先写测试再写实现。包括本地模式和服务器模式两套逻辑都要覆盖——最后 126 个测试全绿才合并。

**案例二：AI 帮我做了一次全库"体检"。**
我让 AI 以只读方式做健康检查，它逐项跑了类型、lint、单测、构建、E2E、依赖审计、性能预算、文档漂移核对，输出一份报告。**它揪出了一个致命的 P0：生产密钥文件被提交进了公开仓库。** 后面我按报告里的处置步骤轮换密钥、移出跟踪、清洗历史。这件事让我特别确信：**AI 在"系统性排查"上比人可靠得多，只要你给它明确的检查清单和只读约束。**

**案例三：性能问题定位到一行代码。**
体检发现首屏 JS 超标 84%。AI 定位到根因：顶栏组件静态引入了动效库，导致 140KB 的库进了入口依赖图。方案是把顶栏拆出主包、配合懒加载，首屏从 367KB 降到 184KB，并把"200KB 预算"写成脚本守住门，以后每次构建自动检查。

---

## 五、我的方法论沉淀（30 秒，拔高）

做完这个项目，我总结出 AI 编程真正要解决的不是"生成代码"，而是三件事：

1. **上下文**：怎么让 AI 在失忆的前提下依然掌握项目约束 → 靠文档工程。
2. **验证**：AI 很会写出"看起来对"的代码 → 靠可执行的验收（测试、构建、性能预算脚本），而不是靠它说什么。
3. **边界**：哪些决策不能让 AI 做 → 不可逆的架构选择、安全红线、合并发布，必须人拍板。

我不觉得 AI 会取代工程师。但这个项目让我看到：**会用 AI 的人，能把一个人的产出拉到一个小团队的量级。前提是你得是个合格的工程师——因为验收标准、架构判断、红线，都只能由人来定。**

---

## 六、30 秒电梯版

> 我独立做了一个考研学习管理网站，已上线。但它的意义在于：这是我用 AI 编程完整跑通的工程实践。我做了三件事——用 `AGENT.md`、术语表、架构文档给 AI 建可维护的长期上下文；用"设计文档 → 实施计划 → 子代理执行 → 我亲自验收"的工作流管住过程；用测试、构建、性能预算这些可执行标准做验收。结果是一个人完成了 1.8 万行代码、196 次提交，还让 AI 帮我做全库体检、揪出了一个生产密钥泄露的 P0 事故。我的结论是：AI 不会取代工程师，但会放大合格工程师的产出。

---

## 附：可能被追问的问题 & 建议回答

**Q：AI 写的代码你怎么保证质量？**
A：三道关。第一，所有输入统一走 Zod 校验，`user_id` 永远从会话取、不接受客户端传入，这是写进文档的红线。第二，可执行验收——单测、tsc、ESLint、Playwright 实测、性能预算脚本，AI 说"完成"不算数。第三，我自己读关键 diff，尤其是涉及安全、数据隔离、不可逆改动的部分。

**Q：AI 会跑偏或者写出幻觉，你怎么处理？**
A：靠"窄上下文 + 任务书"。每个子代理只拿自包含的任务说明和必读文件，缩小它自由发挥的空间。另外我遇到过 AI 声称改了某处但其实没改，所以我现在只认命令输出，不认文字汇报。

**Q：这套方法能迁移到团队协作吗？**
A：能。本质是把"人脑里的项目知识"外化成机器可读、可维护的文档，再配一套统一的验收标准。团队里 AI 产出不一致、新人上手慢，其实都是同一个问题——上下文没有沉淀。我做的东西正好是这个问题的单机版。

**Q：为什么不直接用 v0 / Cursor 这类工具一把梭？**
A：工具能加速写代码，但管不了"这个项目特有的约束"。真正让 AI 在长周期项目里不崩的，是持续的上下文维护和验收集制。工具换哪个都行，方法论得自己有。
