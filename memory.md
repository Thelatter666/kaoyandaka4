# Lessons Learned

## 2026-07-28 密文.md 覆盖事故

### 错误
用户要求"把公钥写到密文.md里"，我未先读取文件确认已有内容，直接执行 `cat pubkey > 密文.md`，导致文件原有所有 API 密钥、Token、SSH 密钥等永久丢失。

### 以后的操作规范

修改/写入**任何已有文件**前，必须：

1. **先读取文件**，确认已有内容
2. **先备份**：`cp file file.bak`
3. **确认意图**：用户说"写到/放入/添加"一律视为**追加**，除非明确说"覆盖/替换"
4. **密钥/配置类文件**操作前必须做备份
