# 全站前端实施建议

> 基于 `需求文档.md` 第十五节要求编制 | 2026-07-20

**说明**：本项目为全新项目，不存在现有前端代码。以下建议作为实施阶段的前瞻性指导，按优先级分为 P0（实施必备）、P1（高收益体验）、P2（精细增强）。每个阶段实施完成后，应逐项对照验证。

---

## P0 — 严重可用性、无障碍、数据理解或关键流程问题

### 1. 首页：倒计时语义明确性

- **位置**：`HomePage > CountdownCard`
- **当前现状**：需求规定"今天不计入剩余天数"，但需求未明确此规则在 UI 上的表达方式。
- **实施建议**：
  - 倒计时卡片必须明确标注计算规则，例如 `2026年12月20日 · 剩余 153 天（不含今日）`
  - 当日期跨过 2026-12-20 后，倒计时应显示 `考试已结束` 或 `0 天`，而不是负数
  - 倒计时数字使用等宽数字（`font-mono`），避免数字跳动时宽度变化
- **预期收益**：消除用户对倒计时计算规则的困惑；防止考试后显示负数的 bug。

### 2. 番茄钟页：无预设时的流程阻断

- **位置**：`PomodoroPage > Step 1`
- **当前现状**：需求规定必须先选择预设才能开始专注，但不允许无预设直接启动。若无预设存在，用户会被完全阻断。
- **实施建议**：
  - 无预设时，番茄钟页显示引导性空状态："还没有学习预设，无法开始专注"
  - 提供直接跳转到预设创建页的 CTA 按钮："创建第一个预设"
  - `PresetCardSelector` 组件在空列表时必须展示此引导，而非白屏或死胡同
- **预期收益**：防止新用户首次使用番茄钟时遇到死胡同；符合需求"空状态应有明确引导"原则。

### 3. 番茄钟：环形计时器无障碍实现

- **位置**：`RingCountdown > SVG Ring`
- **当前现状**：SVG 环形进度默认对屏幕阅读器不可见，仅视觉展示剩余时间。
- **实施建议**：
  - 外容器设置 `role="timer"` 和 `aria-live="polite"`
  - `aria-label` 动态更新为可读描述，例如 `专注剩余 32 分 15 秒` 或 `短休息剩余 4 分 30 秒`
  - 同时提供一个 visually-hidden 的 `<div>` 包含相同的文字信息，作为 fallback
  - `prefers-reduced-motion` 时，移除 SVG stroke-dashoffset 的 CSS transition，仅通过 aria-label 表达更新
  - 剩余时间颜色的变化（如最后 5 分钟变红）必须同时有文字提示
- **预期收益**：屏幕阅读器用户可以独立使用番茄钟；符合 WCAG 4.1.2 和需求"不能仅依赖图形理解状态"。

### 4. 网课导入：解析失败时的用户引导

- **位置**：`ImportModal > Step 2 Preview`
- **当前现状**：导入文本解析可能部分失败或完全失败（用户粘贴格式不符合 `MM:SS` 或 `H:MM:SS`）。
- **实施建议**：
  - 完全无法识别时：显示明确的格式示例和说明，而不是仅显示"解析失败"
  - 部分识别时：未识别行高亮显示行号和原文，并提供"跳过未识别行继续导入"的选项
  - 预览中每行的时长解析结果必须直观展示（如 `31:43 → 31 分 43 秒`），让用户发现解析错误
  - 所有预览数据仅在内存中，确认前不发送任何 API 请求
- **预期收益**：用户在格式错误时有明确的修正方向；防止误解解析结果导致错误数据入库。

### 5. 删除操作：不可逆确认设计

- **位置**：所有删除操作（任务、预设、课程）
- **当前现状**：需求要求删除前二次确认，删除后永久不可恢复，不支持回收站和撤销。
- **实施建议**：
  - 所有删除确认弹窗必须：
    - 明确列出将被删除的内容（名称、关联影响）
    - 使用破坏性按钮样式（红色），与取消按钮有足够视觉差异
    - 网课删除额外说明："已产生的学习记录和统计数据将保留"
    - 按钮文字使用动词："删除"而非"确认"
  - 确认后显示删除中的 loading 状态，防止重复点击
  - 删除失败时显示明确错误 + 重试 + 取消选项
- **预期收益**：防止用户误删重要数据；在不可逆操作前提供充分信息。

### 6. 每日复盘：切换日期时未保存内容保护

- **位置**：`PlanPage > DailyReviewSection`
- **当前现状**：需求规定"切换日期时不得丢失未保存内容"。
- **实施建议**：
  - 当用户在当前日期有未保存的复盘内容时，切换日期前弹出确认提示："你有未保存的复盘内容，切换日期将丢失更改。是否继续？"
  - 提供"保存并切换"、"不保存并切换"、"取消"三个选项
  - 或者更优方案：将未保存内容暂存到内存/state 中，切换回来时恢复
  - 复盘保存按钮显示明确状态：`保存中...`（loading）→ `已保存`（success，短暂停留）→ `保存失败，点击重试`（error）
- **预期收益**：防止用户辛苦写的复盘内容意外丢失，减少挫败感。

### 7. 专注会话：重复完成保护

- **位置**：`POST /api/v1/focus/:id/complete`
- **当前现状**：需求规定"每个专注会话只能完成一次，必须防止重复点击或并发请求生成重复学习记录"。这是关键数据正确性问题。
- **实施建议**：
  - 前端：点击"完成"后立即禁用按钮 + 显示 loading
  - 后端：使用数据库乐观锁（`WHERE status='in_progress'`），若已处理返回 409
  - 前端收到 409 后静默处理（重新查询 active session 状态），不显示错误
- **预期收益**：防止重复学习记录导致统计数据失真（种树计算错误）。

---

## P1 — 高收益体验、信息层级、响应式或一致性问题

### 8. 首页：1366×768 视口无滚动约束

- **位置**：`HomePage` 整体布局
- **当前现状**：需求规定"首页在 1366×768 桌面视口中，包含导航时不应产生纵向滚动"。
- **实施建议**：
  - 导航高度估算：约 56–64px（含 padding）
  - 可用内容高度：约 704–712px
  - 内容区垂直布局估算：
    - 日期 + 问候语：约 40px
    - 倒计时卡：约 120px
    - 今日专注卡：约 200px（含"开始专注"按钮）
    - 今日任务摘要：约 180px（最多 5 条，每条约 36px）
    - 预设概览：约 140px（紧凑卡片）
  - 使用 `height: 100dvh` 配合 `overflow: hidden` 确保不滚动
  - 任务摘要超 5 条时仅显示前 5 条 + "查看全部 →"链接
  - 预设概览超 3 个时仅显示最近 3 个
  - 在小屏设备上移除 `overflow: hidden`，允许自然滚动
- **预期收益**：在目标设备上提供一屏完整的仪表盘体验，无需滚动即可获取所有关键信息。

### 9. 导航：移动端响应式适配

- **位置**：`TopNav` 组件
- **当前现状**：桌面端 7 项导航可水平排列，移动端需要适配方案。
- **实施建议**：
  - 移动端（<768px）：使用底部导航栏 + 汉堡菜单组合
  - 底部导航保留 4 个高频入口（首页、番茄钟、计划、统计）
  - 其余入口（预设、网课、主题切换）收入汉堡菜单或更多菜单
  - 或者：使用顶部可横向滚动（`overflow-x: auto` + `scroll-snap`）的导航条
  - 无论方案如何，确保导航项目触控目标 ≥ 44×44px
  - 当前页面高亮状态必须同时在颜色和形状（下划线/背景色）上表达
- **预期收益**：移动端可用导航，不因导航项目数量导致布局破损。

### 10. 任务排序：拖拽 + 键盘双通道

- **位置**：`PlanPage > TaskList`
- **当前现状**：需求规定"拖拽不能是唯一排序方式，需要键盘可操作替代方案"。
- **实施建议**：
  - 拖拽排序：使用 HTML Drag and Drop API 或轻量库（如 `@dnd-kit/core`）
  - 键盘排序：
    - 每个任务项的排序手柄可聚焦（`tabindex="0"`）
    - 聚焦后按 `Space` 进入排序模式
    - `ArrowUp`/`ArrowDown` 移动项目
    - `Space` 确认位置，`Escape` 取消
  - 同时提供"上移一位"/"下移一位"的隐藏按钮（`sr-only` 但屏幕阅读器可操作）
  - 排序完成后调用 `PATCH /api/v1/tasks/reorder`
  - 排序期间显示温和的保存中反馈
- **预期收益**：键盘用户和屏幕阅读器用户均可独立完成排序操作。

### 11. 学习预设页：创建入口一致性

- **位置**：`PresetsPage`
- **当前现状**：需求规定了 4 种创建入口（数学锁定、英语锁定、408 锁定、通用），每种入口的科目选择行为不同。
- **实施建议**：
  - 页面顶部提供 4 个创建按钮：`创建数学预设`、`创建英语预设`、`创建 408 预设`、`新建预设`
  - 前三个按钮使用 `SubjectBadge` 颜色区分（如数学=蓝色系、英语=橙色系、408=紫色系）
  - 打开创建弹窗时，锁定入口对应的科目字段显示为 readonly badge，不可修改
  - 通用入口则显示完整科目选择下拉框
  - 右下角统一一个 FAB（悬浮操作按钮）作为"新建预设"的快捷入口
  - 编辑预设时始终显示完整科目选择（无锁定）
- **预期收益**：用户快速理解不同入口的行为差异，减少创建时的科目选择错误。

### 12. 番茄钟与预设卡片选择器一致性

- **位置**：`PresetCardSelector`（被番茄钟页、计划页、网课集数启动共用）
- **当前现状**：需求规定所有启动专注的入口统一使用预设卡片选择，不使用下拉菜单。
- **实施建议**：
  - 封装为独立组件 `<PresetCardSelector>`，接受可选 `onStartFocus` 回调
  - 卡片展示：预设名称、科目 badge、时长（如 `45 分钟`）
  - 选中状态使用主色边框 + 轻微缩放（`transform: scale(1.02)`），同时有选中文字标签
  - 卡片按数学、英语、408 三个分组展示，组内按最近使用时间倒序
  - 最近使用的预设显示"最近"标签
  - 加载时显示骨架卡片
  - 嵌入到番茄钟页、计划页启动专注、网课启动学习时行为一致
- **预期收益**：全站统一的预设选择体验，降低认知负担。

### 13. 网课页：7 个分区布局策略

- **位置**：`CoursesPage`
- **当前现状**：需求规定始终显示 7 个课程分区，即使为空。桌面端按 12 栏网格排列，但 7 不是整数因子。
- **实施建议**：
  - 桌面端使用 CSS Grid：
    - 数学、英语：各占 6 栏（第一行，两列）
    - 408·未分类：占 6 栏（第二行左侧）
    - 四个 408 子科目：各占 3 栏（第二行右侧，2×2 小格）
  - 或者采用统一样式：所有分区等宽，使用 `grid-template-columns: repeat(auto-fill, minmax(320px, 1fr))`
  - 每个分区的卡片高度一致（使用 `align-items: stretch`）
  - 空分区显示虚线边框 + 导入按钮，视觉上暗示"可填充"
  - 导入按钮使用次按钮样式，不争夺视觉优先级
- **预期收益**：7 个分区在桌面端视觉平衡，空分区不会被忽略。

### 14. 课程详情页：两条进度条的视觉区分

- **位置**：`CourseDetailPage > CourseStats`
- **当前现状**：需求规定两条进度条——打卡进度（集数完成率）和时长进度（观看时长/总时长）。
- **实施建议**：
  - 两条进度条使用不同的视觉语言：
    - 打卡进度：松绿色填充 + 完成圆点图标，标签为"集数进度 X/Y"
    - 时长进度：朱砂红填充 + 钟表图标，标签为"时长进度 Xh/Xh"
  - 进度条有轻微状态变化动效（完成时填充色短暂变亮再恢复）
  - `prefers-reduced-motion` 时取消动画
  - 两条进度条上下排列，保留足够间距（≥ 16px）
  - 避免仅用颜色区分两条进度条——配合图标和标签文字
- **预期收益**：用户一眼区分两种进度维度，不会混淆。

### 15. 统计页：学习森林空数据体验

- **位置**：`StatisticsPage > ForestVisualization`
- **当前现状**：新用户可能没有任何专注记录，森林为空。
- **实施建议**：
  - 空状态使用温暖的鼓励文案："还没有种下第一棵树。每次专注 1 小时，这里就会长出一棵属于你的学习树 🌿"
  - 显示一棵灰色线稿的树作为视觉占位
  - 提供明确的 CTA："去番茄钟开始第一次专注"
  - 切换到过去有数据的范围时正常展示
  - 日/周/月切换器在空数据时仍可正常操作
  - 累计成果卡片在空数据时显示"0"而非隐藏
- **预期收益**：空状态不让人感到挫败，提供清晰的正向行动指引。

### 16. 统计页：范围导航边界处理

- **位置**：`StatisticsPage > RangeNavigator`
- **当前现状**：需求规定不允许切换到未来范围，但未明确"今天"在日/周/月模式下的边界。
- **实施建议**：
  - 日前向按钮 disabled 条件：当前显示日期 ≥ 今天
  - 周前向按钮 disabled 条件：当前周的最后一天 ≥ 今天（或当前周包含今天）
  - 月前向按钮 disabled 条件：当前月 ≥ 本月
  - disabled 状态使用 `opacity: 0.3` + `cursor: not-allowed` + `aria-disabled="true"`
  - 鼠标悬停时 tooltip 提示："已是最新，无法查看未来数据"
  - 后向按钮始终可用（可回溯历史），直到到达最早数据日
  - 可显示"今天"/"本周"/"本月"快捷按钮一键跳回
- **预期收益**：范围导航边界清晰，用户不会困惑为何按钮不可点击。

### 17. 通用：深色主题独立验证

- **位置**：全局所有页面和组件
- **当前现状**：需求规定深色与浅色主题都需独立验证对比度。深色主题不是简单的"反色"。
- **实施建议**：
  - 深色主题使用设计文档中定义的深墨蓝色系（`#1A2332`），不是纯黑
  - 对所有文字背景组合验证 WCAG AA：
    - 正文 vs 深墨蓝背景 ≥ 4.5:1
    - 朱砂红（`#E74C3C`）vs 深墨蓝背景 ≥ 4.5:1（可能需微调至 `#F06B5C` 或更亮）
    - 松绿 vs 深墨蓝背景 ≥ 4.5:1
  - 焦点 outline 在深色背景下必须可见（使用浅色 outline）
  - 卡片阴影在深色背景下应更淡（`rgba(0,0,0,0.20)`）
  - 边框在深色背景下使用 `rgba(255,255,255,0.08)`
- **预期收益**：深色主题在弱光环境下可用，符合 WCAG AA。

---

## P2 — 视觉精细化和增强项

### 18. 环形倒计时：视觉精细度

- **位置**：`RingCountdown`
- **当前现状**：功能规格已明确方向（环形代替表盘），但视觉细节可以精细优化。
- **实施建议**：
  - 环形线宽建议 8–12px，过细难以感知进度，过粗显得笨重
  - 环形起始位置在 12 点钟方向（顶部），这是最自然的"满"的位置
  - 剩余 ≤ 30 秒时可以有轻柔的脉冲效果（`opacity` 变化），但 `prefers-reduced-motion` 时取消
  - 不同模式的环形颜色：
    - 专注：朱砂红 (`--color-accent-primary`)
    - 短休息：松绿 (`--color-accent-success`)
    - 长休息：深青绿 (`--color-accent-deep-green`)
  - 环形底色使用 `rgba(0,0,0,0.06)`（浅色）/ `rgba(255,255,255,0.06)`（深色）
  - 模式文字使用 `font-mono` 等宽字体显示时间数字，字体大小建议 48–64px
- **预期收益**：计时器视觉精致，与"中文书房感"风格一致。

### 19. 学习森林：树种视觉设计

- **位置**：`StatisticsPage > ForestVisualization > TreeIcon`
- **当前现状**：需求规定三种树种（数学树、英语树、408 树），通过文字、图标/形状和标签区分，不仅依赖颜色。
- **实施建议**：
  - 三种树形状不同：
    - 数学树：松树形状（三角形叠加），冷色调（蓝绿）
    - 英语树：阔叶树形状（圆形树冠），暖色调（橙棕）
    - 408 树：果树形状（树冠 + 果实圆点），紫色调
  - 使用 SVG 图标，三种树的轮廓形状明显不同
  - 每个树种下方显示标签文字（"数学 · X 棵"）
  - 未种下的树显示为灰色线框轮廓（虚线描边）
  - 树种不应是 emoji，使用自定义 SVG
  - 树的大小随累计数量增长可略微放大（但不超过合理范围）
- **预期收益**：树木区分度高，色盲用户也能通过形状和标签区分。

### 20. 首页："开始专注"CTA 处理

- **位置**：`HomePage > TodayFocusCard`
- **当前现状**：首页主操作是"开始专注"，但番茄钟页要求必须先选预设。首页应有流畅的启动体验。
- **实施建议**：
  - 若已有预设：点击直接唤起 `PresetCardSelector` 弹窗（弹窗从番茄钟页复用）
  - 选中预设后显示本次 DurationSelector + "开始专注"按钮
  - 专注开始后显示小型的 RingCountdown（嵌入式，位于首页顶部或专注卡片内）
  - 若首次使用无预设：显示引导性提示 + "创建第一个预设"按钮
  - "开始专注"按钮使用主按钮样式（朱砂红背景），是整个首页最突出的元素
- **预期收益**：首页专注入口体验无缝，不打断用户流程。

### 21. 主题切换水波动效

- **位置**：`ThemeToggle`
- **当前现状**：需求规定了从按钮位置向全页扩散的明暗水波效果。
- **实施建议**：
  - 使用 View Transitions API 时：通过 CSS `::view-transition-old(root)` 和 `::view-transition-new(root)` + `clip-path: circle()` 实现
  - 动效曲线：`cubic-bezier(0.4, 0, 0.2, 1)`（Material ease-out），总时长约 320ms
  - 降级方案（不支持 View Transitions）：CSS `transition` on `background-color`、`color`、`border-color`，约 180ms
  - 水波扩散原点为按钮的几何中心
  - `prefers-reduced-motion` 时直接切换，无中间帧
  - 切换不阻塞页面交互——用户可以在过渡期间继续操作
- **预期收益**：主题切换从功能操作升级为愉悦的微交互。

### 22. 通用：微交互与过渡一致性

- **位置**：所有交互元素
- **当前现状**：需求要求动效只使用低成本属性（opacity、transform、进度填充）。
- **实施建议**：
  - 按钮 hover：`background-color` 轻微变暗 + 轻微 `scale(1.02)`（仅主按钮）
  - 按钮 active：`scale(0.98)`
  - 卡片 hover：`transform: translateY(-2px)` + 阴影增强
  - Modal 打开：`opacity 0→1` + `transform: scale(0.95→1)`，约 200ms
  - Toast 出现：`opacity 0→1` + `transform: translateY(8px→0)`，约 200ms
  - 进度条填充：`transition: width 300ms ease-out`
  - 所有过渡使用一致的 duration 体系（180ms / 320ms）
  - `prefers-reduced-motion` 时全部取消，直接切换最终状态
- **预期收益**：全站交互感受一致、精致；同时尊重用户的减少动效偏好。

### 23. 加载状态：骨架屏策略

- **位置**：所有数据加载区域
- **当前现状**：需求要求显示加载状态，但未规定具体形式。
- **实施建议**：
  - 卡片列表区域使用骨架卡片（柔和渐变动画，`background: linear-gradient(90deg, ...)`）
  - 统计图表区域使用简洁的骨架矩形
  - 避免使用闪烁的 spinner 作为主加载方式（除小型按钮 loading 外）
  - 骨架屏动画：只在 `prefers-reduced-motion: no-preference` 时使用 shimmer，否则显示静态灰色占位
  - 首次加载使用骨架屏，后续刷新可使用顶部细微进度条
- **预期收益**：感知性能更好，加载过程不那么突兀。

### 24. 空状态：全站统一模式

- **位置**：所有空数据场景
- **当前现状**：需求多处提到空状态引导，但各页面独立实施可能导致不一致。
- **实施建议**：
  - 封装 `<EmptyState>` 组件作为全站统一空状态模式：
    - `icon`: 相关 SVG 图标（无数据时使用相关隐喻图标）
    - `title`: 鼓励性标题
    - `description`: 下一步操作说明
    - `action`: CTA 按钮（可选）
  - 所有页面的空状态使用这个组件，确保文案语调、视觉风格一致
  - 空状态卡片使用虚线边框或更低的视觉权重
- **预期收益**：全站空状态体验一致，降低各页面分别设计的成本。

---

## 附录：建议验证时间节点

| 阶段 | 验证内容 |
|---|---|
| 阶段 1（基础框架）后 | P0-6（复盘保护）、P1-9（导航响应式）、P1-17（深色主题 Token）、P2-22（微交互一致） |
| 阶段 3（番茄钟）后 | P0-2（无预设阻断）、P0-3（环形计时器无障碍）、P0-7（重复完成保护） |
| 阶段 4（每日任务）后 | P0-6（复盘切换保护）、P1-10（键盘排序） |
| 阶段 5（首页）后 | P0-1（倒计时语义）、P1-8（1366×768 约束）、P2-20（CTA 流程） |
| 阶段 6（网课）后 | P0-4（导入失败引导）、P0-5（删除确认）、P1-13（7 分区布局）、P1-14（双进度条） |
| 阶段 7（统计）后 | P1-15（森林空数据）、P1-16（范围导航边界）、P2-19（树种视觉） |
| 阶段 8（验收）前 | 全面验证 P0-1 至 P2-24 所有适用项 |
