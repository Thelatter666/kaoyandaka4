# 服务器巡检报告 · 2026-09-15 01:29 (GMT+8)

> 巡检对象：腾讯云轻量应用服务器 `lhins-8cm230w8`
> 地域 `ap-chengdu` · 公网 `118.24.164.3` · 宝塔 Linux 面板

## 结论：⚠️ 监测目标无法通过 API 定位（监控失效）

| 检查项 | 结果 |
|---|---|
| 凭证加载 | ✅ 已加载，`TENCENTCLOUD_SECRET_ID/KEY` 生效，API 鉴权通过 |
| 目标地域实例 | ❌ `DescribeInstances(ap-chengdu)` → `TotalCount = 0` |
| 全地域兜底扫描 | ❌ 14 个轻量地域**均无任何实例** |
| 运行状态 InstanceState | ⚠️ 无法获取（实例未找到） |
| 到期时间 ExpiredTime | ⚠️ 无法获取（实例未找到） |
| 公网出带宽 | ⚠️ 无法获取（实例未找到，跳过） |
| 邮件告警 | ❌ 未发送：Agent 邮箱尚未开通（not_bound） |

## 说明

1. **实例未找到**：当前凭证对应的腾讯云账号下，在全部 14 个轻量可用地域中均没有任何轻量应用服务器。这意味着
   `lhins-8cm230w8` 可能属于**另一套账号/SecretId**，或实例 ID、地域信息有误。本次无法判定其 `InstanceState`
   与 `ExpiredTime`，属于比"已知非 RUNNING"更严重的监控盲区。
2. **未发邮件原因**：agent-mail 经 `GetMe` 确认处于"尚未开通"状态（`status=not_bound`），告警链路不可用，
   故未按"非 RUNNING / 临期 ≤10 天"逻辑发信。请勿重试发信。
3. **合规说明**：本环境无法直连服务器端口、禁原始 TCP，全程仅依据腾讯云 API 判断，未使用 curl/ping/SSH。

## 建议处理

- 核实实例归属：确认 `lhins-8cm230w8` 是否归属当前 `env.sh` 凭证对应的账号；若否，请切换正确凭证或修正实例 ID。
- 开通 Agent 邮箱（入口：客户端「更多 - 我的邮箱」），否则后续任何真实异常都无法通过邮件触达。
- 修正后下次巡检即可正常判定运行状态与到期日，并触发续费/离线告警。
