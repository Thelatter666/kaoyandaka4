# 砚台考研打卡 · 前端重设计文档

> 主题：变体A · 极光玻璃（Aurora Glass） | 2026-07-20
> 适用范围：client/ 全部页面与组件。后端 API、数据模型、统计口径、业务逻辑不变。
> 本文件是全站 UI 重构的唯一设计依据，各实施阶段必须严格对照执行。

---

## 一、主题概念与设计原则

### 1.1 概念

「极光玻璃」：清晨书房的光线与空气感。晨雾白底上，大团柔和的极光渐变光斑（粉紫→雾蓝→蜜金）静静漂浮；所有卡片、导航、弹窗都是半透磨砂玻璃，浮在光斑之上，随内容层叠出光影。深色主题是同一空间的午夜版本：午夜靛蓝底色上漂浮着北极光绿紫渐变光晕。

两大核心视觉：

- **番茄钟「光晕核心」**：悬浮玻璃圆盘上的多层同心环。外环是带同色辉光的进度环，中环一圈刻度珠记录今日已完成轮次，中心是超大渐变等宽数字。完成一轮瞬间，光环粒子爆散。
- **学习森林「玻璃花房」**：微缩温室场景。2.5D 低多边形 SVG 小树按种植时间排布在苔藓地面上，玻璃罩斜向反光，尘埃光斑缓缓漂浮，天空随真实时间昼夜渐变。

### 1.2 设计原则（优先级从高到低）

1. **功能与数据不变**：所有交互流程、API 调用、统计口径保持现状，仅重构视觉与呈现层交互。
2. **视效聚焦**：全站视效预算集中在番茄钟与森林两处；其余页面做"安静的精致"——材质统一、层级清晰、微交互克制。
3. **玻璃一致性**：全站只允许三种玻璃材质档位（见 4.3），禁止随机发明新的模糊/透明组合。
4. **动效有意义**：每个动效必须表达因果（按压、完成、切换、加载），禁止纯装饰动画；动效只用 `opacity` / `transform` / 进度填充属性。
5. **可访问性不可协商**：WCAG AA 对比度、键盘可达、focus 可见、`prefers-reduced-motion` 全量降级、状态不只用颜色表达。
6. **零重型依赖**：视觉全部以 CSS/SVG 实现；仅新增 `lucide-react` 图标库；不引入动画库、不引入 three.js。

---

## 二、设计令牌（Design Tokens）

全部令牌定义于 `client/src/styles/tokens.css`，组件只允许引用令牌，禁止书写裸色值。

### 2.1 浅色主题（默认）

| 令牌 | 值 | 用途 |
|---|---|---|
| `--color-bg-page` | `#F7F8FA` | 晨雾白页面底色 |
| `--color-text-primary` | `#1D2939` | 正文主色 |
| `--color-text-secondary` | `#475467` | 次要文字 |
| `--color-text-muted` | `#667085` | 弱化文字（AA 下限） |
| `--color-text-inverse` | `#FFFFFF` | 深底上的文字 |
| `--color-accent-primary` | `#E05545` | 珊瑚红：辉光、大图形、渐变 |
| `--color-accent-primary-strong` | `#B93A2C` | 主按钮底/正文红字（白字对比 5.6:1） |
| `--color-accent-primary-light` | `rgba(224,85,69,0.10)` | 主色浅底 |
| `--color-accent-success` | `#2FA36B` | 松绿：图形、进度 |
| `--color-accent-success-strong` | `#1E7A4E` | 成功态文字/实底（白字对比 5.4:1） |
| `--color-accent-deep-green` | `#0E6655` | 长休息 |
| `--color-accent-warning` | `#D9730D` | 警示（AA 达标） |
| `--color-accent-danger` | `#C43228` | 破坏性操作 |
| `--color-glass-bg` | `rgba(255,255,255,0.55)` | 玻璃卡底色 |
| `--color-glass-bg-strong` | `rgba(255,255,255,0.72)` | 高层级玻璃（弹窗/导航） |
| `--color-glass-border` | `rgba(255,255,255,0.65)` | 玻璃顶部高光边 |
| `--color-border` | `rgba(16,24,40,0.08)` | 常规描边 |
| `--color-border-focus` | `#B93A2C` | 焦点环 |
| `--color-subject-math` | `#3E7CB1` | 数学 |
| `--color-subject-english` | `#D9730D` | 英语 |
| `--color-subject-408` | `#7C4DBE` | 408 |
| `--color-subject-math-bg` | `rgba(62,124,177,0.10)` | 数学浅底 |
| `--color-subject-english-bg` | `rgba(217,115,13,0.10)` | 英语浅底 |
| `--color-subject-408-bg` | `rgba(124,77,190,0.10)` | 408 浅底 |

极光光斑（浅色）：粉紫 `#C7B8F5`、雾蓝 `#A8CBF5`、蜜金 `#F5D9A8`、青绿 `#A8E6CF`，以 radial-gradient + 大模糊（blur 80px）低频透明度（0.35–0.5）呈现。

### 2.2 深色主题

| 令牌 | 值 | 说明 |
|---|---|---|
| `--color-bg-page` | `#101828` | 午夜靛蓝 |
| `--color-text-primary` | `#F2F4F7` | |
| `--color-text-secondary` | `#C4CBD6` | |
| `--color-text-muted` | `#98A2B3` | |
| `--color-text-inverse` | `#101828` | |
| `--color-accent-primary` | `#F4786A` | 提亮珊瑚红（对底 6.1:1） |
| `--color-accent-primary-strong` | `#F4786A` | 深色下文字与图形同用 |
| `--color-accent-primary-light` | `rgba(244,120,106,0.14)` | |
| `--color-accent-success` | `#3ECF8E` | 提亮松绿 |
| `--color-accent-success-strong` | `#3ECF8E` | |
| `--color-accent-deep-green` | `#2DD4BF` | |
| `--color-accent-warning` | `#F5A45C` | |
| `--color-accent-danger` | `#F4786A` | |
| `--color-glass-bg` | `rgba(255,255,255,0.06)` | 烟熏玻璃 |
| `--color-glass-bg-strong` | `rgba(23,32,51,0.78)` | 高层级玻璃 |
| `--color-glass-border` | `rgba(255,255,255,0.10)` | |
| `--color-border` | `rgba(255,255,255,0.09)` | |
| `--color-border-focus` | `#F4786A` | |
| `--color-subject-math` | `#6FB1E8` | |
| `--color-subject-english` | `#F5A45C` | |
| `--color-subject-408` | `#B490E8` | |
| `--color-subject-math-bg` | `rgba(111,177,232,0.14)` | |
| `--color-subject-english-bg` | `rgba(245,164,92,0.14)` | |
| `--color-subject-408-bg` | `rgba(180,144,232,0.14)` | |

深色极光光斑：极光绿 `#34D399`、极光紫 `#A78BFA`、极光蓝 `#60A5FA`，透明度 0.10–0.16。

### 2.3 形状 / 阴影 / 动效令牌

```css
/* 圆角 */
--radius-sm: 10px;  --radius-md: 14px;  --radius-card: 24px;
--radius-lg: 28px;  --radius-full: 9999px;
/* 玻璃模糊 */
--blur-glass: 16px;  --blur-glass-strong: 20px;
/* 阴影（浅色/深色分别定义） */
--shadow-glass-sm: 0 2px 12px rgba(16,24,40,0.06), inset 0 1px 0 var(--color-glass-border);
--shadow-glass-md: 0 8px 32px rgba(16,24,40,0.10), inset 0 1px 0 var(--color-glass-border);
--shadow-glass-lg: 0 16px 48px rgba(16,24,40,0.14), inset 0 1px 0 var(--color-glass-border);
/* 深色下改为 rgba(0,0,0,0.28 / 0.36 / 0.44) */
/* 彩色辉光（仅核心视觉使用） */
--glow-primary: 0 0 24px rgba(224,85,69,0.45);
--glow-success: 0 0 24px rgba(47,163,107,0.45);
/* 动效 */
--dur-fast: 160ms;  --dur-med: 240ms;  --dur-slow: 400ms;
--ease-out: cubic-bezier(0.22, 1, 0.36, 1);
/* 间距沿用 4/8 体系：xs4 sm8 md16 lg24 xl32 2xl48 */
/* z-index：aurora 0 / content 1 / nav 50 / modal 200 / toast 300 / tooltip 400 */
```

---

## 三、字体系统

| 角色 | 字体 | 用途 |
|---|---|---|
| `--font-heading` | `'Noto Serif SC', 'Songti SC', serif` | 页面标题、卡片大标题（保留中文书房气质） |
| `--font-body` | `'Noto Sans SC', 'PingFang SC', sans-serif` | 正文 |
| `--font-mono` | `'JetBrains Mono', 'SF Mono', monospace` | 计时数字、统计数字、日期范围 |

字级：12/14/16/18/20/24/30/36，计时器专用 64px（移动 52px）。标题 600–700，正文 400，标签 500。数字一律 `font-variant-numeric: tabular-nums`。字体通过 Google Fonts CSS 引入，`font-display: swap`。

---

## 四、材质、图标与动效语言

### 4.1 玻璃材质三档（全站唯一合法组合）

| 档位 | 背景 | 模糊 | 阴影 | 用途 |
|---|---|---|---|---|
| `glass-1` | `--color-glass-bg` | `--blur-glass` + `saturate(1.4)` | `--shadow-glass-sm` | 常规卡片、列表项 |
| `glass-2` | `--color-glass-bg-strong` | `--blur-glass-strong` + `saturate(1.4)` | `--shadow-glass-md` | 导航、弹窗、下拉、番茄钟圆盘 |
| `glass-3` | 深底反转（`rgba(29,41,57,0.72)` / 深色 `rgba(16,24,40,0.85)`） | 20px | `--shadow-glass-lg` | Toast、tooltip |

玻璃元素统一 `border: 1px solid var(--color-border)` + `backdrop-filter`（带 `-webkit-` 前缀）。不支持 backdrop-filter 时降级为不透明实底（`@supports not` 回退：`background: var(--color-bg-card-solid)`，浅色 `#FFFFFF`、深色 `#1D2939`，新增令牌）。

### 4.2 极光背景（AuroraBackground 组件）

- 固定定位全屏层 `position: fixed; inset: 0; z-index: 0; pointer-events: none`，页面内容 `z-index: 1`。
- 4 个光斑（浅色：粉紫/雾蓝/蜜金/青绿；深色：绿/紫/蓝），每个为 `radial-gradient(circle, color 0%, transparent 70%)`，尺寸 40–60vmax，`filter: blur(80px)`，分布于左上、右上、左下、右下。
- 光斑做极缓慢漂移：两个光斑一组，`transform: translate()` 往返 36s/48s，`ease-in-out` infinite alternate。`prefers-reduced-motion` 下静止。
- 光斑层不得遮挡交互（pointer-events none）、不得引起重排（只动 transform）。

### 4.3 图标体系（lucide-react）

新增依赖 `lucide-react`，全部 emoji 图标替换为 SVG（stroke 1.75，尺寸默认 16/18/20）。映射表：

| 场景 | 旧 | 新图标 |
|---|---|---|
| 导航-首页 | 🏠 | `Home` |
| 导航-计划 | 📋 | `ClipboardList` |
| 导航-预设 | ⚙️ | `SlidersHorizontal` |
| 导航-番茄钟 | 🍅 | `Timer` |
| 导航-网课 | 📺 | `MonitorPlay` |
| 导航-统计 | 🌳 | `Trees` |
| 主题切换 | — | `Sun` / `Moon` |
| 本期成果 | 📊 | `BarChart3` |
| 累计成果 | 🏆 | `Trophy` |
| 学习记录 | 📖 | `BookOpen` |
| 空态/新芽 | 🌿🌱 | `Sprout` |
| 删除 | — | `Trash2` |
| 编辑 | — | `Pencil` |
| 完成 | — | `Check` / `CheckCircle2` |
| 重要图钉 | — | `Pin` / `PinOff` |
| 拖拽手柄 | — | `GripVertical` |
| 开始/暂停 | — | `Play` / `Pause` |
| 休息 | — | `Coffee` |
| 长休息 | — | `Armchair` |
| 加减微调 | — | `Minus` / `Plus` |
| 范围导航 | ←→ | `ChevronLeft` / `ChevronRight` |
| 错误/警告 | — | `AlertCircle` / `AlertTriangle` |
| 重试 | — | `RefreshCw` |
| 关闭 | — | `X` |
| 倒计时 | — | `Hourglass` |
| 专注 | — | `Focus` |

森林树木、番茄钟粒子为定制 SVG/canvas，不用图标库。

### 4.4 动效语言

| 场景 | 规格 |
|---|---|
| 按钮 hover | `translateY(-1px)` + 阴影增强，`--dur-fast` |
| 按钮 active | `scale(0.97)` |
| 卡片 hover（可点击卡） | `translateY(-2px)` + 阴影升至 md，`--dur-med` |
| 弹窗/菜单出现 | `opacity 0→1` + `scale(0.96→1)`，200ms `--ease-out` |
| Toast 出现 | `opacity` + `translateY(8px→0)`，200ms |
| 页签/模式切换 | 选中指示器 `transform` 滑动 240ms |
| 进度条 | `width` 填充 300ms ease-out（允许进度填充属性） |
| 列表项入场 | 逐项 `opacity`+`translateY(6px)`，每项延迟 40ms，最多 8 项 |
| 主题切换 | View Transitions 圆形扩散 180ms；降级 180ms 颜色淡变 |
| 加载 | 玻璃骨架 shimmer（background-position 动画 1.6s） |

`prefers-reduced-motion`：全部动画/过渡取消（沿用 global.css 全局 0.01ms 规则），状态直接呈现。

---

## 五、核心视觉规格 A：番茄钟「光晕核心」

重写 `client/src/components/timer/RingCountdown.tsx`，配套重构 `PomodoroPage.tsx`。

### 5.1 结构（由外向内）

1. **玻璃圆盘容器**：直径桌面 400px / 移动 `min(88vw, 400px)`，正圆，`glass-2` 材质，`border-radius: 50%`，`position: relative`。
2. **外环·进度环**（SVG viewBox `0 0 360 360`）：`r=160`，`stroke-width=12`，`strokeLinecap=round`，12 点起笔顺时针消减。颜色按模式：专注 `var(--color-accent-primary)`、短休 `var(--color-accent-success)`、长休 `var(--color-accent-deep-green)`。辉光：`filter: drop-shadow(0 0 10px currentColor)`（用 `color` 跟随模式色，避免动画模糊半径）。底环 `r=160` 同色 8% 透明。
3. **中环·轮次刻度珠**：`r=136` 圆周均布 24 颗小圆点（`r=3`）。今日已完成轮次为 n 时，前 n 颗点亮：填充模式色 + 同模式辉光；其余填充 `var(--color-border)`。珠子数量常量 `MAX_BEADS=24`，超过则全部点亮。数据由页面传入 prop `completedRoundsToday`。
4. **内环·装饰细环**：`r=112`，`stroke-width=1.5`，`var(--color-border)`，纯装饰 `aria-hidden`。
5. **中心内容**（绝对居中 HTML 层，非 SVG 文本）：
   - 剩余时间：`--font-mono`，桌面 64px / 移动 52px，700，渐变填充：`background: linear-gradient(135deg, 模式色, 模式色提亮 30%)` + `-webkit-background-clip: text`（深色主题直接用纯色，确保对比度）。
   - 模式文字：14px 500，跟随模式色：`专注中 / 短休息 / 长休息`。
   - 预设名+科目：12px `--color-text-muted`，一行省略。
6. **低时警示**：剩余 ≤300s 且为专注模式：进度环与数字渐变为金红（`--color-accent-primary` → `#F5A45C`），环做呼吸脉动（`opacity 0.7↔1`，2s infinite）。reduced-motion 下仅变色不脉动。
7. **完成粒子**：会话自然结束/提前完成时，在圆盘中心触发一次粒子爆散：轻量 canvas（≤20 粒子，模式色，重力+淡出，1.2s 后销毁 canvas）。取消/休息结束不触发。reduced-motion 跳过。封装为 `BurstParticles.tsx`。

### 5.2 可访问性

- 容器 `role="timer"`、`aria-live="polite"`、`aria-label="专注中 剩余 32 分 15 秒"`（每分钟粒度更新，避免频繁播报）。
- SVG 全部 `aria-hidden`，信息由 HTML 文本层与 aria-label 承载。
- 刻度珠变化在 aria-label 中不播报；页面另设 sr-only 文本"今日已完成 N 轮"。

### 5.3 页面布局（PomodoroPage）

- 未开始：页面中央为预设卡选择区（沿用既有流程），选中后时长选择器收进一张 `glass-2` 横条卡，主 CTA「开始专注」为全站最大主按钮。
- 进行中：圆盘居中为绝对主角，操作区（完成/取消/休息入口）置于圆盘正下方一排；页面其余内容淡出（`opacity 0.4 → 可交互性保留`，不做全屏遮罩）。完成后按现有规则展示继续/短休/长休入口，第 4 轮高亮长休（辉光描边）。
- 移动端：圆盘缩放，操作按钮纵向堆叠，全部 ≥44px。

---

## 六、核心视觉规格 B：学习森林「玻璃花房」

重写 `StatisticsPage.tsx` 森林区，新建 `client/src/components/forest/` 目录。

### 6.1 场景结构（自底向上 z 序）

容器：`glass-1` 大卡片（`--radius-lg`），内部场景区桌面 16:9（min-height 340px）/ 移动 4:3，`overflow: hidden`，`border-radius: 20px`。

1. **天空层 SkyLayer**：`linear-gradient` 按本地小时取 5 档——黎明(5–7) `#F5D9A8→#A8CBF5`、上午(8–11) `#BFDCF5→#E8F3FB`、午后(12–16) `#A8CBF5→#D9ECFA`、黄昏(17–19) `#F5B88A→#C7B8F5`、夜晚(20–4) `#2B3A55→#4A5A7A`。深色主题整体叠加 `rgba(16,24,40,0.35)` 压暗。切换用 CSS transition 1s。
2. **远山层 HillLayer**：2 条 SVG path 剪影（`fill` 科目无关的中性蓝灰，opacity 0.10/0.16），贴场景底部。
3. **地面层 GroundLayer**：底部 28% 高度，苔藓渐变（`#7FB069→#5E9152`，夜晚压暗）+ 若干小草叶 SVG 点缀。
4. **树层 TreeLayer**：本期树木按"科目分组、组内按种植先后"排布：数学左区、英语中区、408 右区；透视规则——同区内索引 i 的树 `scale = 0.55 + 0.45 * (i / max(1, count-1))`（早种的远而小，晚种的近而大），底部基线错落 ±8px。每棵树是 `<button type="button">`（≥44px 触控区，视觉树形居中），`aria-label="数学 第2棵树"`。
5. **玻璃罩反光 GlassSheen**：场景顶部一道弧形高光（SVG ellipse，白 12%→0 渐变）+ 左上→右下对角反光带（白 6%），`pointer-events: none`。
6. **尘埃光斑 DustMotes**：6 个 4–8px 模糊圆点（白 40%），`translate` 缓慢漂浮 12–20s alternate infinite，错相位。reduced-motion 静止。

### 6.2 树种 SVG 规格（低多边形插画风，≈64×80 viewBox `0 0 64 80`）

- **数学·松树 TreePine**：3 层三角层叠（冷青绿 `#3E8E7E / #4FA891 / #6FC3AC`），短棕干。形状语言：尖锐、挺劲。
- **英语·阔叶树 TreeBroadleaf**：不规则圆冠 2 色面（`#5FA85C / #7FC47B`）+ 弯棕干。形状语言：圆润、舒展。
- **408·果树 TreeFruit**：圆冠（`#8E6FC7 / #A98FDB`）+ 3–4 颗果实圆点（`#F4786A`）。形状语言：圆冠+果点。
- **幼苗态 Sapling**：该科本期 0 棵时占位显示一株：两片小叶 + 虚线圆坑，`opacity 0.5`。
- 每科树下有地面阴影椭圆（黑 8%）。三种树同时以形状+树冠色+图例文字区分，不依赖单一颜色。
- 图例：场景右上角玻璃小胶囊图例（树形小图标+科目名+棵数）。

### 6.3 交互与信息

- hover/focus 树木 → 露珠 tooltip（`glass-3`，水滴圆角 `border-radius: 16px 16px 16px 4px`）："第 N 棵 · 数学"，键盘 focus 同样触发，`Escape` 关闭。
- 场景标题条：左侧"本期学习森林"+范围文字；右侧图例。
- 场景下方数据条（玻璃横条）：本期专注时长 / 完成次数 / 树木总数 / 各科距下一棵树剩余时长（四等分，tabular-nums）。
- 容量：单期树木 >24 棵时渲染最近 24 棵，场景右下角"+N 棵"玻璃角标。
- 空态（本期 0 棵且累计 0 棵）：场景内只显示三株幼苗占位 + 居中引导文案"还没有种下第一棵树"+ CTA「去番茄钟开始第一次专注」。
- 降级：reduced-motion 下尘埃静止、天空静态；不支持 backdrop-filter 时场景卡片用实底。所有数据同时以文字呈现（数据条不依赖图形）。

---

## 七、全局布局与导航

### 7.1 AuroraBackground 接入

`App.tsx` 根节点挂载一次，全页面共享（见 4.2）。

### 7.2 TopNav（重写 `components/layout/TopNav.tsx`）

- 悬浮胶囊导航：`position: sticky; top: 12px`，`margin: 12px auto 0`，`max-width: 960px`，`glass-2`，`border-radius: var(--radius-full)`，高 56px，z-index 50。
- 左侧品牌"砚台考研"（宋体 700 18px），中间 6 项导航（lucide 图标 16px + 文字），右侧主题切换。
- 当前页：`background: var(--color-accent-primary-strong)` 白字胶囊 + 微辉光；非当前：`--color-text-secondary`，hover 玻璃浅底。
- `aria-current="page"` 保留。
- 响应式：`<960px` 导航项文字隐藏（图标-only，`aria-label` 保留，触控区 ≥44px）；`<720px` 品牌名隐藏；导航横向不滚动、不溢出（6 图标可放下）。

### 7.3 PageShell

`max-width: 1120px`，水平 padding 24px（移动 16px），`z-index: 1`，页面标题区统一：宋体 30px + 引导文案 14px secondary。

### 7.4 ThemeToggle

保留水波扩散逻辑；按钮改为玻璃圆形图标按钮（`Sun`/`Moon`，44px），`aria-label="切换主题"`。

---

## 八、各页面重设计

### 8.1 首页 HomePage

- 布局：12 栏网格 → 首行：考试倒计时卡（8 栏）+ 今日专注卡（4 栏）；次行：今日任务摘要（7 栏）+ 学习预设概览（5 栏）。保持 1366×768 无纵向滚动。
- 倒计时卡：`glass-1`，超大等宽数字（48px tabular-nums）"153"+ "天"小字，副标题"距 2026 年 12 月 20 日（不含今日）"，`Hourglass` 图标；卡片左上叠一小团蜜金光斑（`overflow:hidden` 内装饰）。
- 今日专注卡：主 CTA「开始专注」（primary 大按钮，辉光 hover）；有进行中会话时显示迷你进度环（复用 RingCountdown 的 120px 简化模式 `variant="mini"`，无刻度珠）。
- 任务摘要/预设概览：玻璃列表行，重要任务 `Pin` 图标 + 主色左边条；空态用 EmptyState 规范。
- 数字全部 tabular-nums；标题宋体。

### 8.2 计划页 PlanPage

- 「今天/明天」页签改为玻璃分段控件（滑块指示器 240ms 滑动）。
- 任务项 `TaskItem`：glass-1 行卡，完成态=松绿 `CheckCircle2` + 文字删除线 + 透明度 0.62（不只靠颜色：图标+删除线）；重要=`Pin` 填充主色；拖拽手柄 `GripVertical` 44px 触控区；键盘排序按钮保留 sr-only。
- 新建任务行：玻璃输入条，聚焦时描边主色辉光。
- 复盘区：glass-1 卡 + 多行输入，保存状态用图标+文字（`RefreshCw` 转动/`Check`/重试），切换日期保护逻辑不变。
- 次日处理弹窗沿用 Modal 新材质。

### 8.3 学习预设页 PresetsPage

- 三科分组标题：科目色小图标 + 宋体标题 + 组内数量胶囊。
- `PresetCard`：glass-1，顶部 3px 科目色渐变条（形状+颜色双重编码：左侧同时显示科目 lucide 图标），名称宋体 18px，时长等宽大数字 + 分钟小字，hover 上浮，"最近"标签玻璃胶囊。
- `DurationSelector`：快捷钮（25/45/60）玻璃胶囊组，-5/+5 为 44px 圆钮；当前值等宽 36px。
- 创建入口：顶部 4 按钮（三科锁定+通用），科目色区分；弹窗 glass-2。

### 8.4 番茄钟页 PomodoroPage

见第五节。预设选择阶段：`PresetCardSelector` 卡片网格（glass-1，选中态=主色 2px 描边+辉光+左上角 `CheckCircle2`），无预设时 EmptyState 引导创建。

### 8.5 网课页 CoursesPage

- 7 分区：`grid-template-columns: repeat(auto-fill, minmax(320px, 1fr))`，分区卡 glass-1：标题行（科目图标+名称+进度百分比）+ 双进度条 + 课程列表/空态导入按钮。
- 空分区：虚线描边玻璃卡 + 「导入 [分区名] 课程」次按钮（`Plus` 图标）。
- 双进度条：玻璃槽（`rgba` 底）+ 渐变填充（集数=松绿系，时长=珊瑚系），各带图标+文字标签，不只用颜色区分。
- 导入弹窗：三步（粘贴→预览→确认）步骤指示器；未识别行警示色高亮+行号。

### 8.6 课程详情页 CourseDetailPage

- 顶部课程信息卡：glass-1，课程名宋体 24px + 科目徽章 + 双进度条 + 统计数字行。
- 集数列表：glass-1 行，完成圆点 28px（`Check` 填充松绿），标题/原始时长/开始学习按钮（`Play`）；完成行动效 160ms。
- 删除入口：danger 幽灵按钮，确认弹窗按 4.1 glass-2。

### 8.7 统计页 StatisticsPage

- 顶部：标题"学习森林"（宋体 30px）+ 引导文案；日/周/月玻璃分段控件；范围导航器（`ChevronLeft/Right` 44px 玻璃圆钮 + 等宽日期范围 + 「今天」胶囊）；禁用态 `opacity 0.35` + `aria-disabled`。
- 主体依次为：玻璃花房场景（第六节）→ 数据条 → 本期成果卡 + 累计成果卡（glass-1，图标+大数字网格）→ 学习记录时间线（按日分组，glass-1 行卡：科目徽章/标题/时长/时刻）。
- 全部 emoji 移除；加载=场景骨架 shimmer；错误=ErrorState+重试。

---

## 九、基础组件规格（components/ui/）

| 组件 | 规格 |
|---|---|
| `Button` | variants：`primary`（accent-primary-strong 底白字+同色投影，hover 微浮+辉光）/ `glass`（glass-1+描边）/ `ghost`（透明，hover 浅底）/ `danger`（danger 底白字）；sizes sm 36px / md 44px / lg 52px；active `scale(0.97)`；loading=`Loader2` 旋转+禁用 |
| `Card` | glass-1，padding 档位 16/24/32，`hoverable` prop 开启上浮 |
| `Modal` | 遮罩 `rgba(16,24,40,0.32)`+blur 8px；面板 glass-2 `--radius-card`，进出 200ms scale+fade；`role="dialog"` `aria-modal`，焦点圈禁与 Esc 关闭保留 |
| `ConfirmDialog` | 基于 Modal；destructive 用 danger 按钮，文案动词化 |
| `Toast` | glass-3，`border-radius: var(--radius-md)`，成功 `CheckCircle2` / 错误 `AlertCircle` + 文字，`aria-live="polite"`，3.5s 自动消失 |
| `EmptyState` | 96px 玻璃圆（glass-1+内图标 40px secondary）+ 宋体标题 20px + 描述 14px + 可选 CTA |
| `ErrorState` | 同构，`AlertCircle` danger 色 + 重试按钮 |
| `LoadingState` | 玻璃骨架条 shimmer（1.6s），附 sr-only"加载中" |
| `ProgressBar` | 槽 `var(--color-border)` 6px 圆角；填充渐变（可配双色），`role="progressbar"` + aria-valuenow |
| `SubjectBadge` | 科目色玻璃底胶囊：lucide 科目图标 14px + 文字 12px 500（数学 `Sigma`/英语 `BookA`/408 `Cpu`） |
| `SkipLink` | 保留逻辑，样式改 glass-2 胶囊 |

---

## 十、可访问性与响应式硬性规范

1. 所有文本/背景组合 ≥4.5:1（大字号 ≥3:1），双主题分别核验；本文件令牌已按此选定，实施时不得降低。
2. 焦点环：2px `var(--color-border-focus)` + 2px offset，深色下同样可见；禁止移除。
3. 交互目标 ≥44×44px；图标按钮必有 `aria-label`；树、刻度珠等图形信息必有文字等价物。
4. 状态表达必须"颜色+图标/文字/形状"双通道（完成态、选中态、禁用态、错误态）。
5. `prefers-reduced-motion`：动画全停；粒子、漂浮、脉动、shimmer 全部静止；数据与状态完整可读。
6. 响应式断点 375/768/1024/1440；移动端无横向滚动；桌面容器 max-width 1120。
7. 键盘：Tab 顺序合理；森林树木、刻度珠 tooltip、任务排序均可键盘操作；弹窗焦点圈禁。

---

## 十一、实施阶段与验收

### 阶段划分（每阶段独立 commit，中文消息）

| 阶段 | 范围 | commit |
|---|---|---|
| 0 | 本文档 | `docs: 新增前端重设计文档（极光玻璃主题）` |
| 1 | tokens/global/utilities + ui/ 基础组件 + lucide-react | `feat: 重建极光玻璃设计令牌与基础组件` |
| 2 | AuroraBackground + TopNav + PageShell + ThemeToggle | `feat: 重构全局布局导航与极光背景` |
| 3 | RingCountdown + PomodoroPage | `feat: 重构番茄钟页为光晕核心视觉` |
| 4 | forest/ 组件 + StatisticsPage | `feat: 重构统计页学习森林为玻璃花房场景` |
| 5 | HomePage + PlanPage + tasks/presets 共享件 | `feat: 重构首页与计划页视觉` |
| 6 | PresetsPage + CoursesPage + CourseDetailPage + courses/ | `feat: 重构预设与网课页面视觉` |
| 7 | 全面验收修复 | `fix: 全站视觉验收与一致性打磨` |

### 每阶段验证门槛

`npm run lint`、`npm run build`、`npm test` 全通过（阶段 1 可只跑 lint+build）；改动页面对应的既有测试不许破坏。

### 最终验收清单（阶段 7）

- [ ] `npm test` / `npm run test:e2e` / `npm run lint` / `npm run build` / `git diff --check` 全通过
- [ ] 全站无 emoji 图标（grep 校验）
- [ ] 双主题对比度抽查：正文/主按钮/科目徽章/导航/禁用态
- [ ] reduced-motion 下：环静止可读、森林静态完整、主题切换无动画
- [ ] 1366×768 首页无纵向滚动；375px 无横向滚动
- [ ] 键盘走查：导航→番茄钟启动→森林树木 tooltip→任务排序→弹窗
- [ ] 既有功能回归：预设锁定科目、时长 5–120/5 分钟倍数、休息 5/15 固定、会话恢复、重复完成保护、森林统计口径、删除二次确认

---

# v2 · 布局栅格与动效系统（2026-07-20 补充）

> 本节为最高优先级规范，与上文冲突时以本节为准。目标：布局从"等大卡片平铺"升级为 **Bento 主次网格**；动效从"hover 上移"升级为 **页面级编排**。视觉基因（极光玻璃、色彩、字体、番茄钟/森林核心视觉）不变。

## 十二、Bento 栅格系统

### 12.1 栅格工具（utilities.css 提供）

- `.bento-grid`：`display: grid; grid-template-columns: repeat(12, 1fr); gap: 24px`（<720px 时 gap 16px 且全部子项垮 12 栏单列）。
- 跨度类：`.bento-span-3 / -4 / -5 / -6 / -7 / -8 / -12`（`grid-column: span N`）；行跨类：`.bento-row-2`（`grid-row: span 2`）。
- 断点：<1024px 时 span-3/4/5 变 span-6，span-7/8 变 span-12；<720px 全部 span-12。各页可用页面级 CSS 微调，但必须复用 `.bento-grid` 基座。

### 12.2 卡片层级（二级）

| 层级 | 材质 | 圆角 | 内边距 | 附加 | 用途 |
|---|---|---|---|---|---|
| 主角卡 `.card--hero` | glass-2 | 28px | 32px | 内嵌一团极光光斑（绝对定位、blur 36px、opacity ≤0.5、overflow hidden） | 每页唯一视觉主角 |
| 功能卡 | glass-1 | 24px | 24px | — | 其余卡片 |

规则：每页**有且仅有一个**主角卡；主角卡必须比周围卡明显更大（跨度或行跨）；标题区不计入主角。

### 12.3 PageShell v2

- 容器 max-width **1200px**（原 1120），水平 padding 24px（移动 16px）。
- 统一页头组件 `.page-header`：宋体 30px 标题 + 14px secondary 副文案 + 右侧操作槽（`.page-header__actions`），下边距 32px。
- 区块垂直节奏：页头→主体 32px；主体内部区块间隔 40px。

### 12.4 各页 Bento 构图

| 页面 | 构图（桌面 12 栏） | 主角 |
|---|---|---|
| 首页 | 今日专注卡 span 8 / 倒计时卡 span 4 + row-2（高卡） / 今日任务 span 8 / 预设概览 span 12（横条卡，预设横向排列） | 今日专注卡 |
| 统计 | 森林玻璃花房 span 12（全景） / 三张本期数据卡 span 4×3 / 累计成果+时间线 | 森林花房 |
| 番茄钟 | 非 bento：圆盘舞台居中（背后聚灯光斑+大留白），预设选择为底部 dock 横排卡；进行中辅助信息收进单一侧卡 | 圆盘 |
| 计划 | 任务区 span 8 主列 / 复盘卡 span 4 sticky 侧栏（<1024px 转为上下堆叠） | 任务区 |
| 预设 | 页头操作槽放创建按钮；每科分组为 bento 网格，**最近使用**的预设放大为特色卡（span 6-8），其余 span 4 | 最近使用卡 |
| 网课 | 7 分区错落：数学(6) 英语(6) / 408未分类(4) 数据结构(4) 计组(4) / 操作系统(6) 网络(6) | 无（多主体） |
| 课程详情 | 课程信息卡 span 12 hero 化（玻璃罩光斑+大标题+双进度条横排） / 集数列表 | 信息卡 |

## 十三、页面级动效编排

### 13.1 动效令牌（tokens.css 新增）

```css
--ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);   /* 弹性强调 */
--dur-page-exit: 140ms;
--dur-page-enter: 320ms;
--dur-rise: 420ms;
--stagger-step: 70ms;
```

### 13.2 页面切换过渡（App.tsx 状态机）

- hash 变化时：旧页面容器加 `.page-exit`（140ms，`opacity→0` + `translateY(-6px)`），结束后卸载旧页、挂载新页并加 `.page-enter`（320ms `var(--ease-out)`，`opacity 0→1` + `translateY(10px→0)`）。
- 实现要点：用 state 保存 `displayedPage` 与 `phase`（'idle'|'exit'），`setTimeout` 140ms 后切换；快速连续切换时取消未完成计时器直接切换；`prefers-reduced-motion` 与状态机异常时降级为立即切换。
- 过渡容器为页面根 `<div>`，`key = page + 参数` 保证重挂载；不影响各页内部数据请求时机。

### 13.3 入场编排（stagger）

- 工具类 `.reveal`：`animation: rise-in var(--dur-rise) var(--ease-out) both; animation-delay: calc(var(--i, 0) * var(--stagger-step))`。
- `@keyframes rise-in`：`from { opacity: 0; transform: translateY(12px); }`。
- 用法：页面主体区块/卡片按阅读顺序设置 `style={{ '--i': n }}`，n 从 0 递增，**每页最多 8 个**（delay ≤490ms）；列表内大量项不做 stagger。
- 入场编排只在页面首次挂载时播放一次（与 13.2 的 page-enter 叠加时，reveal 延迟自然衔接，无需额外协调）。

### 13.4 微交互升级

- 卡片 hover（可点击卡/主角卡）：`translateY(-3px)` + 阴影升一档 + **光泽扫过**（伪元素斜向白色渐变带，`transform: translateX(-120%)→120%`，500ms，仅 hover 时播放一次）。
- 弹性强调：刻度珠点亮、徽章/ toast 弹出、CTA 按压回弹使用 `--ease-spring`；按压 `scale(0.96)`、释放回弹。
- 页面切换与 stagger 之外的既有动效（分段控件滑块、进度条、模态）保持不变。

### 13.5 降级

`prefers-reduced-motion`：页面直接切换无过渡、`.reveal` 无动画（opacity 1 直出）、光泽扫过与弹性回弹取消、其余沿用全局 0.01ms 规则。

## 十四、v2 验收标准

1. 任意切换页面有 exit/enter 过渡感，无闪烁无白屏。
2. 每页打开时元素按节奏依次入场（≤8 个）。
3. 每页一眼可指出唯一视觉主角；卡片不再等大平铺。
4. 首页 1366×768 无纵向滚动；375/768/1024/1440 无横向滚动。
5. reduced-motion 下全部动画静止、内容立即可读。
6. 对比度不因布局调整退化（AA）。
