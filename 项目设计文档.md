# 砚台考研打卡 — 项目设计文档

> 基于 `需求文档.md` 编制 | 2026-07-20 | 版本 1.0

---

## 目录

1. [项目概述与技术架构](#1-项目概述与技术架构)
2. [项目目录结构](#2-项目目录结构)
3. [数据库设计](#3-数据库设计)
4. [API 接口设计](#4-api-接口设计)
5. [共享校验 Schema（Zod）](#5-共享校验-schema)
6. [前端架构设计](#6-前端架构设计)
7. [核心流程设计](#7-核心流程设计)
8. [视觉与主题系统](#8-视觉与主题系统)
9. [可访问性设计](#9-可访问性设计)
10. [测试策略](#10-测试策略)
11. [实施计划（分阶段）](#11-实施计划)
12. [风险与约束记录](#12-风险与约束记录)

---

## 1. 项目概述与技术架构

### 1.1 项目定位

个人本地使用的考研学习管理网站。目标用户：1 人（开发者本人）。考试日期：**2026-12-20**（硬编码）。

### 1.2 技术栈

| 层 | 技术选型 | 说明 |
|---|---|---|
| 前端 | React 18 + TypeScript + Vite | SPA，不使用路由框架（React Router 可选），按需引入 |
| 后端 | Express 4 + TypeScript | RESTful API，`/api/v1` 根路径 |
| 数据库 | MySQL 8 + mysql2/promise | 本地实例，DBeaver 管理 |
| 校验 | Zod（前后端共享） | `shared/` 目录，前后端各自引用 |
| 单元/集成测试 | Vitest | 数据库集成测试使用独立 `TEST_DB_NAME` |
| E2E | Playwright | 冒烟测试，mock fixture 优先 |
| Lint | ESLint | `npm run lint` |
| 凭据管理 | `.env`（不提交 Git） | 仅 `.env.example` 提交 |

### 1.3 架构拓扑

```
┌──────────────────────────┐
│   Browser (localhost)    │
├──────────────────────────┤
│  React SPA (Vite)        │
│  - 浅色/深色主题         │
│  - 本地存储（主题偏好）  │
│  - SPA 路由（hash 或内存）│
└────────┬─────────────────┘
         │ HTTP (localhost:5173 → proxy → :3001)
┌────────▼─────────────────┐
│  Express Server (:3001)  │
│  - /api/v1/* 路由        │
│  - Zod 校验              │
│  - 错误处理中间件        │
└────────┬─────────────────┘
         │ mysql2/promise
┌────────▼─────────────────┐
│  MySQL 8 (本地)          │
│  - 凭据来自 .env         │
│  - DBeaver 管理          │
└──────────────────────────┘
```

### 1.4 全局约束

- **不做**：账号、云同步、协作、社交、付费、批量删除、回收站、撤销删除
- **日期格式**：`YYYY-MM-DD`（字符串，接口入参/出参均使用）
- **时长**：整数秒（数据库存储），分钟（前端展示、快捷选择）
- **时间戳**：ISO 8601 UTC 字符串
- **实体标识**：稳定 UUID（v4），用于 JSON 备份合并去重
- **数据库迁移**：不新增迁移工具；初始 Schema 作为建表脚本一次性执行
- **凭据安全**：仅 `.env`，不提交、不写入前端、不写入备份 JSON、不出现在错误信息中

---

## 2. 项目目录结构

```
kaoyandaily/
├── client/                        # React + Vite 前端
│   ├── src/
│   │   ├── api/                   # API 调用封装（fetch 包装）
│   │   ├── components/            # 共享 UI 组件
│   │   │   ├── ui/                # 原子组件（Button, Card, Modal, Input...）
│   │   │   ├── layout/            # 布局组件（TopNav, PageShell）
│   │   │   ├── presets/           # 预设相关组件（PresetCard, DurationSelector）
│   │   │   ├── timer/             # 计时器组件（RingCountdown）
│   │   │   ├── tasks/             # 任务组件（TaskItem, TaskList）
│   │   │   ├── courses/           # 网课组件（CourseCard, ImportModal）
│   │   │   └── statistics/        # 统计组件（ForestView, ResultsCard）
│   │   ├── pages/                 # 页面组件
│   │   │   ├── HomePage.tsx
│   │   │   ├── PlanPage.tsx
│   │   │   ├── PresetsPage.tsx
│   │   │   ├── PomodoroPage.tsx
│   │   │   ├── CoursesPage.tsx
│   │   │   ├── CourseDetailPage.tsx
│   │   │   └── StatisticsPage.tsx
│   │   ├── hooks/                 # 自定义 Hooks
│   │   │   ├── useApi.ts          # 通用 API hook（loading/error/data）
│   │   │   ├── useFocusSession.ts # 专注会话管理
│   │   │   ├── useTheme.ts        # 主题切换
│   │   │   ├── useCountdown.ts    # 倒计时逻辑
│   │   │   └── useKeyboardSort.ts # 键盘可操作排序
│   │   ├── styles/
│   │   │   ├── tokens.css         # 设计 Token（CSS 自定义属性）
│   │   │   ├── global.css         # 全局样式、字体引入
│   │   │   └── utilities.css      # 工具类
│   │   ├── utils/
│   │   │   ├── date.ts            # 日期工具函数
│   │   │   ├── duration.ts        # 时长格式化
│   │   │   └── accessibility.ts   # 无障碍辅助
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── index.html
│   ├── vite.config.ts
│   └── tsconfig.json
│
├── server/                        # Express 后端
│   ├── src/
│   │   ├── routes/                # 路由处理
│   │   │   ├── presets.ts
│   │   │   ├── tasks.ts
│   │   │   ├── reviews.ts
│   │   │   ├── focus.ts
│   │   │   ├── courses.ts
│   │   │   └── statistics.ts
│   │   ├── db/
│   │   │   ├── connection.ts      # 数据库连接池
│   │   │   ├── schema.sql         # 初始建表 SQL
│   │   │   └── seed.ts            # 测试种子数据（可选）
│   │   ├── middleware/
│   │   │   ├── errorHandler.ts    # 全局错误处理
│   │   │   ├── validate.ts        # Zod 校验中间件
│   │   │   └── cors.ts            # CORS 配置
│   │   ├── utils/
│   │   │   ├── uuid.ts            # UUID 生成
│   │   │   └── date.ts            # 日期处理
│   │   └── index.ts               # 入口：Express 启动
│   └── tsconfig.json
│
├── shared/                        # 前后端共享
│   └── src/
│       ├── schemas/               # Zod 校验 Schema
│       │   ├── preset.ts
│       │   ├── task.ts
│       │   ├── focus.ts
│       │   ├── course.ts
│       │   ├── statistics.ts
│       │   └── common.ts          # SubjectEnum, SubSubjectEnum, DurationMinutes...
│       └── types/                 # 派生 TypeScript 类型
│           └── index.ts           # z.infer<...> 导出
│
├── e2e/                           # Playwright E2E
│   ├── tests/
│   │   ├── home.spec.ts
│   │   └── statistics.spec.ts
│   ├── fixtures/
│   └── playwright.config.ts
│
├── .env.example                   # 凭据模板（提交 Git）
├── .env                           # 实际凭据（不提交）
├── .gitignore
├── package.json                   # 根 package.json（workspace 脚本）
├── vitest.config.ts               # Vitest 配置
├── eslint.config.js               # ESLint 配置
├── 需求文档.md
└── 项目设计文档.md
```

---

## 3. 数据库设计

### 3.1 表结构总览

共 6 张核心表：

```
study_presets ──────┐
daily_tasks          │
daily_reviews        │
online_courses ──┐   │
course_episodes ─┘   │
focus_sessions ──────┤
study_records ───────┘ (快照引用以上实体)
```

### 3.2 建表 SQL

```sql
-- ============================================================
-- 1. study_presets（学习预设）
-- ============================================================
CREATE TABLE study_presets (
    id          CHAR(36) PRIMARY KEY,              -- UUID v4
    name        VARCHAR(200) NOT NULL,
    subject     ENUM('math','english','408') NOT NULL,
    sub_subject ENUM('data_structure','computer_organization','operating_system','computer_network') NULL,
    duration_minutes INT NOT NULL,                 -- 5–120，5 的倍数
    last_used_at DATETIME NULL,
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    CONSTRAINT chk_preset_duration CHECK (duration_minutes >= 5 AND duration_minutes <= 120 AND duration_minutes % 5 = 0)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- ============================================================
-- 2. daily_tasks（每日任务）
-- ============================================================
CREATE TABLE daily_tasks (
    id          CHAR(36) PRIMARY KEY,
    task_date   DATE NOT NULL,                     -- YYYY-MM-DD
    content     VARCHAR(500) NOT NULL,
    subject     ENUM('math','english','408') NOT NULL,
    sub_subject ENUM('data_structure','computer_organization','operating_system','computer_network') NULL,
    is_completed BOOLEAN NOT NULL DEFAULT FALSE,
    is_important BOOLEAN NOT NULL DEFAULT FALSE,
    sort_order  INT NOT NULL DEFAULT 0,
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_tasks_date (task_date),
    INDEX idx_tasks_date_important_sort (task_date, is_important DESC, sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- ============================================================
-- 3. daily_reviews（每日复盘）
-- ============================================================
CREATE TABLE daily_reviews (
    id          CHAR(36) PRIMARY KEY,
    review_date DATE NOT NULL,                     -- YYYY-MM-DD，每天最多一条
    content     TEXT NOT NULL,
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    UNIQUE INDEX idx_review_date (review_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- ============================================================
-- 4. online_courses（网课）
-- ============================================================
CREATE TABLE online_courses (
    id          CHAR(36) PRIMARY KEY,
    name        VARCHAR(200) NOT NULL,
    subject     ENUM('math','english','408') NOT NULL,
    sub_subject ENUM('data_structure','computer_organization','operating_system','computer_network') NULL,
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- ============================================================
-- 5. course_episodes（网课集数）
-- ============================================================
CREATE TABLE course_episodes (
    id              CHAR(36) PRIMARY KEY,
    course_id       CHAR(36) NOT NULL,
    title           VARCHAR(500) NOT NULL,
    duration_seconds INT NOT NULL,                 -- 解析后的秒数
    duration_text   VARCHAR(20) NOT NULL,          -- 用户粘贴的原始时长文本
    sort_order      INT NOT NULL DEFAULT 0,        -- 原始导入顺序
    is_completed    BOOLEAN NOT NULL DEFAULT FALSE,
    completed_at    DATETIME NULL,
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_episodes_course (course_id),
    CONSTRAINT fk_episodes_course FOREIGN KEY (course_id)
        REFERENCES online_courses(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- ============================================================
-- 6. focus_sessions（专注会话）
-- ============================================================
CREATE TABLE focus_sessions (
    id                      CHAR(36) PRIMARY KEY,
    preset_id               CHAR(36) NULL,         -- 原预设可能被删除，不设 FK
    preset_name_snapshot    VARCHAR(200) NOT NULL,
    subject_snapshot        ENUM('math','english','408') NOT NULL,
    sub_subject_snapshot    ENUM('data_structure','computer_organization','operating_system','computer_network') NULL,
    planned_duration_seconds INT NOT NULL,
    actual_duration_seconds INT NULL,              -- 完成时填入
    started_at              DATETIME NOT NULL,
    planned_end_at          DATETIME NOT NULL,      -- started_at + planned_duration
    completed_at            DATETIME NULL,
    status                  ENUM('in_progress','completed','cancelled') NOT NULL DEFAULT 'in_progress',
    source                  ENUM('pomodoro','plan','course') NOT NULL,
    course_episode_id       CHAR(36) NULL,          -- 关联网课集数
    task_id                 CHAR(36) NULL,          -- 关联任务
    created_at              DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at              DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_focus_status (status),
    INDEX idx_focus_started_at (started_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- ============================================================
-- 7. study_records（学习记录）
-- ============================================================
CREATE TABLE study_records (
    id                      CHAR(36) PRIMARY KEY,
    -- 快照字段（脱离原实体独立存在）
    preset_name_snapshot    VARCHAR(200) NOT NULL,
    subject_snapshot        ENUM('math','english','408') NOT NULL,
    sub_subject_snapshot    ENUM('data_structure','computer_organization','operating_system','computer_network') NULL,
    actual_duration_seconds INT NOT NULL,
    -- 关联（可选，允许被关联实体删除后置 NULL）
    focus_session_id        CHAR(36) NULL,
    task_id                 CHAR(36) NULL,
    course_episode_id       CHAR(36) NULL,
    -- 网课快照（来源为 course_video 时有值）
    course_name_snapshot    VARCHAR(200) NULL,
    episode_title_snapshot  VARCHAR(500) NULL,
    -- 分类
    source                  ENUM('focus_session','course_video') NOT NULL,
    -- 可编辑
    notes                   TEXT NULL,
    -- 时间戳
    created_at              DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at              DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_records_created (created_at),
    INDEX idx_records_subject (subject_snapshot),
    INDEX idx_records_source (source)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 3.3 实体关系说明

```
study_presets ──→ focus_sessions (preset_id, 快照字段)
daily_tasks   ──→ focus_sessions (task_id)
course_episodes ──→ focus_sessions (course_episode_id)
focus_sessions ──→ study_records (focus_session_id, source='focus_session')
course_episodes ──→ study_records (course_episode_id, source='course_video')
```

**关键设计原则：**

1. `study_records` 是最终的、"不可变"的学习记录存储。所有快照字段在写入时从关联实体 copy，后续实体删除/修改不影响记录。
2. `focus_sessions` 是临时会话状态。`in_progress` 状态用于页面刷新恢复；`completed`/`cancelled` 为终态。
3. `course_episodes` 通过 `ON DELETE CASCADE` 随课程删除；但 `study_records` 中的快照不受影响。

### 3.4 统计去重口径（数据计算规则）

| 记录来源 | 计入统计规则 |
|---|---|
| `focus_session` 且未关联网课 | 以 `actual_duration_seconds` 计入 |
| `focus_session` 且关联了网课 | 以 `actual_duration_seconds` 计入（专注时长），不额外计入视频时长 |
| `course_video` 且未关联专注 | 以视频 `duration_seconds` 计入 |
| `course_video` 且关联了专注 | **不计入**（避免重复累计） |
| 课程补录（source=course_video） | **不计入**森林统计 |

---

## 4. API 接口设计

所有接口以 `/api/v1` 为前缀。请求体与响应体均为 `application/json`。

### 4.1 学习预设

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/v1/presets` | 获取所有预设（按数学/英语/408 分组，组内按 `last_used_at DESC`） |
| `POST` | `/api/v1/presets` | 创建预设 |
| `PUT` | `/api/v1/presets/:id` | 编辑预设 |
| `DELETE` | `/api/v1/presets/:id` | 删除预设（不影响历史学习记录） |

**POST/PUT 请求体：**
```json
{
  "name": "数学上午刷题",
  "subject": "math",
  "subSubject": null,
  "durationMinutes": 45,
  "lockedSubject": "math"       // 可选：从锁定入口创建时传入，后端校验
}
```

### 4.2 每日任务

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/v1/tasks?date=YYYY-MM-DD` | 获取指定日期任务（按重要、排序排列） |
| `POST` | `/api/v1/tasks` | 创建任务 |
| `PUT` | `/api/v1/tasks/:id` | 编辑任务（内容、科目等） |
| `PATCH` | `/api/v1/tasks/:id/toggle` | 切换完成状态 |
| `PATCH` | `/api/v1/tasks/:id/pin` | 切换重要标记 |
| `PATCH` | `/api/v1/tasks/reorder` | 批量更新排序 |
| `DELETE` | `/api/v1/tasks/:id` | 删除任务（二次确认由前端完成） |
| `GET` | `/api/v1/tasks/unfinished?from=YYYY-MM-DD` | 获取前一天未完成任务（用于次日处理） |

**POST 请求体：**
```json
{
  "date": "2026-07-20",
  "content": "完成线代第三章习题",
  "subject": "math",
  "subSubject": null,
  "isImportant": true
}
```

**POST reorder 请求体：**
```json
{
  "date": "2026-07-20",
  "items": [
    { "id": "uuid-1", "sortOrder": 0, "isImportant": true },
    { "id": "uuid-2", "sortOrder": 1, "isImportant": true },
    { "id": "uuid-3", "sortOrder": 0, "isImportant": false }
  ]
}
```

### 4.3 每日复盘

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/v1/reviews?date=YYYY-MM-DD` | 获取指定日期复盘 |
| `PUT` | `/api/v1/reviews` | 创建或更新复盘（upsert by date） |

**PUT 请求体：**
```json
{
  "date": "2026-07-20",
  "content": "今天数学完成度较好..."
}
```

### 4.4 专注会话

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/api/v1/focus/start` | 启动专注会话（必须带 presetId） |
| `POST` | `/api/v1/focus/:id/complete` | 完成专注（含提前完成） |
| `POST` | `/api/v1/focus/:id/cancel` | 取消专注（不记录） |
| `GET` | `/api/v1/focus/active` | 获取当前活跃会话（用于刷新恢复） |

**POST /start 请求体：**
```json
{
  "presetId": "uuid-of-preset",
  "plannedDurationMinutes": 45,
  "source": "pomodoro",
  "courseEpisodeId": null,
  "taskId": null
}
```

**POST /start 响应（201）：**
```json
{
  "id": "session-uuid",
  "presetNameSnapshot": "数学上午刷题",
  "subjectSnapshot": "math",
  "plannedDurationSeconds": 2700,
  "startedAt": "2026-07-20T02:30:00.000Z",
  "plannedEndAt": "2026-07-20T03:15:00.000Z",
  "status": "in_progress",
  "source": "pomodoro"
}
```

**GET /active 响应：**
- 有活跃会话：返回 200 + 会话详情（含 `startedAt`、`plannedEndAt`，前端据此计算剩余时间）
- 无活跃会话：返回 200 + `null`
- 若会话已到期：服务端自动标记为 `completed`，生成 study_record，返回 200 + `null`

### 4.5 网课

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/v1/courses` | 获取所有课程（用于 7 分区展示） |
| `GET` | `/api/v1/courses/:id` | 获取课程详情（含集数列表） |
| `POST` | `/api/v1/courses/parse` | 解析导入文本（预览，不写入） |
| `POST` | `/api/v1/courses` | 确认导入（写入课程与集数） |
| `DELETE` | `/api/v1/courses/:id` | 删除课程及章节 |
| `PATCH` | `/api/v1/courses/:id/episodes/:eid/toggle` | 切换集数完成状态 |

**POST /parse 请求体：**
```json
{
  "rawText": "第一集 概述\n31:43\n第二集 线性表\n59:19\n",
  "subject": "math",
  "subSubject": null
}
```

**POST /parse 响应：**
```json
{
  "episodes": [
    { "title": "第一集 概述", "durationText": "31:43", "durationSeconds": 1903 },
    { "title": "第二集 线性表", "durationText": "59:19", "durationSeconds": 3559 }
  ],
  "totalEpisodes": 2,
  "totalDurationSeconds": 5462,
  "unrecognizedLines": []
}
```

**POST /create 请求体：**
```json
{
  "name": "2026 张宇线性代数",
  "subject": "math",
  "subSubject": null,
  "lockedSubject": "math",
  "lockedSubSubject": null,
  "episodes": [
    { "title": "第一集 概述", "durationText": "31:43", "durationSeconds": 1903 },
    { "title": "第二集 线性表", "durationText": "59:19", "durationSeconds": 3559 }
  ]
}
```

### 4.6 统计

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/v1/statistics/forest?mode=day\|week\|month&date=YYYY-MM-DD` | 学习森林统计 |

**GET 响应结构：**
```json
{
  "mode": "week",
  "rangeStart": "2026-07-14",
  "rangeEnd": "2026-07-20",
  "canGoBack": true,
  "canGoForward": false,
  "period": {
    "totalFocusSeconds": 16200,
    "totalCompletedSessions": 6,
    "totalTrees": 4,
    "treesBySubject": { "math": 2, "english": 1, "408": 1 },
    "remainingSecondsBySubject": { "math": 1800, "english": 2400, "408": 3000 }
  },
  "cumulative": {
    "totalFocusSeconds": 180000,
    "totalTrees": 50
  },
  "records": [
    {
      "date": "2026-07-20",
      "items": [
        {
          "id": "record-uuid",
          "title": "数学上午刷题",
          "subject": "math",
          "subSubject": null,
          "durationSeconds": 2700,
          "time": "2026-07-20T02:30:00.000Z",
          "source": "focus_session"
        }
      ]
    }
  ]
}
```

### 4.7 全局错误响应格式

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "预设时长必须为 5 的倍数",
    "details": [
      { "field": "durationMinutes", "message": "必须为 5 的倍数" }
    ]
  }
}
```

常见错误码：
- `VALIDATION_ERROR` — Zod 校验失败
- `NOT_FOUND` — 实体不存在
- `CONFLICT` — 重复操作（如重复完成同一会话）
- `FUTURE_DATE` — 请求未来日期
- `INVALID_MODE` — 不支持的统计模式

---

## 5. 共享校验 Schema

位于 `shared/src/schemas/`，前后端通过相对路径或 TypeScript path alias 引用。

### 5.1 common.ts — 基础定义

```typescript
import { z } from 'zod';

export const SubjectEnum = z.enum(['math', 'english', '408']);
export type Subject = z.infer<typeof SubjectEnum>;

export const SubSubjectEnum = z.enum([
  'data_structure',
  'computer_organization',
  'operating_system',
  'computer_network',
]);
export type SubSubject = z.infer<typeof SubSubjectEnum>;

export const DateString = z.string().regex(/^\d{4}-\d{2}-\d{2}$/, '日期格式必须为 YYYY-MM-DD');

export const DurationMinutes = z
  .number()
  .int('必须为整数')
  .min(5, '最短 5 分钟')
  .max(120, '最长 120 分钟')
  .refine((n) => n % 5 === 0, '必须为 5 的倍数');

// 时长选择器限制（前端组件校验 + API 校验均使用）
export const DURATION_QUICK_OPTIONS = [25, 45, 60] as const;
export const DURATION_STEP = 5;
export const DURATION_MIN = 5;
export const DURATION_MAX = 120;

// 休息时长常量
export const SHORT_BREAK_MINUTES = 5;
export const LONG_BREAK_MINUTES = 15;
export const LONG_BREAK_AFTER_ROUNDS = 4;
```

### 5.2 preset.ts — 学习预设

```typescript
import { z } from 'zod';
import { SubjectEnum, SubSubjectEnum, DurationMinutes } from './common';

export const CreatePresetSchema = z.object({
  name: z.string().min(1, '名称不能为空').max(200),
  subject: SubjectEnum,
  subSubject: SubSubjectEnum.optional(),
  durationMinutes: DurationMinutes,
  // lockedSubject: 如果从锁定入口创建，传入以让后端校验
  lockedSubject: SubjectEnum.optional(),
});

export const UpdatePresetSchema = CreatePresetSchema.omit({ lockedSubject: true }).partial();
```

### 5.3 task.ts — 每日任务

```typescript
import { z } from 'zod';
import { SubjectEnum, SubSubjectEnum, DateString } from './common';

export const CreateTaskSchema = z.object({
  date: DateString,
  content: z.string().min(1, '任务内容不能为空').max(500),
  subject: SubjectEnum,
  subSubject: SubSubjectEnum.optional(),
  isImportant: z.boolean().default(false),
});

export const UpdateTaskSchema = CreateTaskSchema.partial();

export const ReorderItemsSchema = z.object({
  date: DateString,
  items: z.array(z.object({
    id: z.string().uuid(),
    sortOrder: z.number().int().min(0),
    isImportant: z.boolean(),
  })),
});
```

### 5.4 focus.ts — 专注会话

```typescript
import { z } from 'zod';
import { DurationMinutes } from './common';

export const StartFocusSchema = z.object({
  presetId: z.string().uuid('预设 ID 无效'),
  plannedDurationMinutes: DurationMinutes,
  source: z.enum(['pomodoro', 'plan', 'course']),
  courseEpisodeId: z.string().uuid().optional(),
  taskId: z.string().uuid().optional(),
});

export const FocusSourceEnum = z.enum(['pomodoro', 'plan', 'course']);

// 不允许自定义休息时长
export const BreakTypeEnum = z.enum(['short', 'long']);
// API 不暴露 breakDuration 参数，休息时长由服务端常量决定
```

### 5.5 course.ts — 网课

```typescript
import { z } from 'zod';
import { SubjectEnum, SubSubjectEnum } from './common';

const EpisodeInput = z.object({
  title: z.string().min(1, '标题不能为空'),
  durationText: z.string().min(1),
  durationSeconds: z.number().int().positive('时长必须 > 0'),
});

export const ParseImportSchema = z.object({
  rawText: z.string().min(1, '导入文本不能为空'),
  subject: SubjectEnum,
  subSubject: SubSubjectEnum.optional(),
});

export const CreateCourseSchema = z.object({
  name: z.string().min(1).max(200),
  subject: SubjectEnum,
  subSubject: SubSubjectEnum.optional(),
  lockedSubject: SubjectEnum.optional(),       // 分区导入锁定校验
  lockedSubSubject: SubSubjectEnum.optional(), // 分区导入锁定校验
  episodes: z.array(EpisodeInput).min(1, '至少需要一集'),
});
```

### 5.6 statistics.ts — 统计

```typescript
import { z } from 'zod';
import { DateString, SubjectEnum, SubSubjectEnum } from './common';

export const StatisticsQuerySchema = z.object({
  mode: z.enum(['day', 'week', 'month'], { message: '仅支持 day / week / month' }),
  date: DateString,
});

// 响应 schema（前后端共享，确保结构一致）
export const PeriodResultSchema = z.object({
  totalFocusSeconds: z.number(),
  totalCompletedSessions: z.number(),
  totalTrees: z.number(),
  treesBySubject: z.record(SubjectEnum, z.number()),
  remainingSecondsBySubject: z.record(SubjectEnum, z.number()),
});

export const LearningRecordItemSchema = z.object({
  id: z.string().uuid(),
  title: z.string(),
  subject: SubjectEnum,
  subSubject: SubSubjectEnum.nullable(),
  durationSeconds: z.number(),
  time: z.string(), // ISO 8601
  source: z.enum(['focus_session', 'course_video']),
});

export const ForestResponseSchema = z.object({
  mode: z.enum(['day', 'week', 'month']),
  rangeStart: DateString,
  rangeEnd: DateString,
  canGoBack: z.boolean(),
  canGoForward: z.boolean(),
  period: PeriodResultSchema,
  cumulative: z.object({
    totalFocusSeconds: z.number(),
    totalTrees: z.number(),
  }),
  records: z.array(z.object({
    date: DateString,
    items: z.array(LearningRecordItemSchema),
  })),
});
```

---

## 6. 前端架构设计

### 6.1 路由设计

使用简单 SPA 路由（hash 路由或 React Router 的 `HashRouter`），不依赖服务端渲染路由。

| Hash 路径 | 页面组件 | 导航标签 |
|---|---|---|
| `#/` | `HomePage` | 首页 |
| `#/plan` | `PlanPage` | 计划 |
| `#/presets` | `PresetsPage` | 学习预设 |
| `#/pomodoro` | `PomodoroPage` | 番茄钟 |
| `#/courses` | `CoursesPage` | 网课 |
| `#/courses/:id` | `CourseDetailPage` | （网课详情） |
| `#/statistics` | `StatisticsPage` | 统计 |

### 6.2 组件树（关键页面）

#### HomePage
```
HomePage
├── TopNav (active: 首页)
├── DateDisplay ("2026年7月20日 周一")
├── CountdownCard
│   ├── ExamDateLabel ("2026 年 12 月 20 日")
│   └── DaysRemaining ("153 天")
├── TodayFocusCard
│   ├── EmptyState ("还没有今天的专注记录")
│   └── CTAButton ("开始专注" → 唤起 PresetCardSelector)
├── TodayTasksSummary (最多 5 条，重要优先)
│   └── TaskSummaryItem[]
└── PresetsOverview (最近使用 3 个预设)
    └── PresetCard[] (紧凑模式)
```

#### PlanPage
```
PlanPage
├── TabBar (今天 | 明天)
├── UnfinishedTaskModal (条件渲染：次日首次打开且有昨天未完成任务)
│   └── UnfinishedTaskItem[] (各带：顺延 / 放弃 / 标记完成)
├── TaskSection (重要)
│   └── TaskItem[] (可拖拽 + 键盘排序)
│       ├── PinButton (图钉)
│       ├── InlineEditText (点击原地编辑)
│       ├── SubjectBadge
│       ├── CompleteCheckbox
│       └── DeleteButton
├── TaskSection (普通)
│   └── TaskItem[]
├── AddTaskForm
│   ├── TextInput (任务内容)
│   ├── SubjectSelect (math/english/408)
│   ├── SubSubjectSelect (408 时显示)
│   └── SubmitButton
└── DailyReviewSection
    ├── DateLabel
    ├── TextArea (自由文本)
    └── SaveIndicator (保存中/已保存/失败+重试)
```

#### PomodoroPage
```
PomodoroPage
├── Step 1: PresetCardSelector (无预设选中时)
│   └── PresetCard[] (点击选中)
├── Step 2: DurationAdjustor (选中预设后)
│   ├── PresetInfoDisplay (预设名称、科目、原始时长)
│   ├── QuickButtons (25 / 45 / 60)
│   ├── StepAdjustor (-5 / +5)
│   └── StartButton ("开始专注")
├── Active Session View (专注运行中)
│   ├── RingCountdownTimer
│   │   ├── SVG Ring (Progress Circle)
│   │   ├── TimeDisplay (MM:SS)
│   │   └── ModeLabel (专注中 / 短休息 / 长休息)
│   ├── RoundCounter (当前第 X 轮)
│   ├── CompleteEarlyButton
│   └── CancelButton
├── Post-Focus Actions (专注结束后)
│   ├── ContinueButton ("继续专注" → 回到 Step 1)
│   ├── ShortBreakButton ("短休息 5 分钟")
│   └── NoBreakButton ("不休息" → 回到 Step 1)
└── LongBreakPrompt (第 4 轮完成后)
    └── LongBreakButton ("一键开始长休息 15 分钟")
```

#### CoursesPage
```
CoursesPage
├── GlobalImportButton ("导入网课")
├── CourseZone × 7 (固定分区)
│   ├── ZoneHeader (分区名)
│   ├── ImportButton ("导入 [分区名] 课程") — 空分区时
│   └── CourseCard[]
│       ├── CourseName
│       ├── SubjectBadge
│       ├── EpisodeCount ("12 集 / 8 小时 30 分")
│       ├── LastStudiedEpisode
│       ├── ProgressBar (打卡进度)
│       ├── ProgressBar (时长进度)
│       └── DeleteButton (→ 确认弹窗)
└── ImportModal
    ├── Step 1: PasteArea
    ├── Step 2: PreviewTable
    │   ├── RecognizedEpisodes
    │   ├── UnrecognizedLines (高亮警告)
    │   └── Summary (总集数、总时长)
    └── Step 3: ConfirmButton
```

#### StatisticsPage
```
StatisticsPage
├── PageHeader ("学习森林")
├── GuidanceText
├── ModeSwitcher (日 | 周 | 月)
├── RangeNavigator (← 今天/本周/本月 →)
│   └── 未来范围时 forward 按钮 disabled
├── ForestVisualization
│   ├── TreeGrid (三栏：数学树 / 英语树 / 408 树)
│   │   └── TreeIcon[] (每棵树一个 SVG 图标)
│   └── NextTreeProgress (每个科目：距离下一棵树还有 X 分钟)
├── PeriodResultsCard
│   ├── FocusDuration ("本期专注 4.5 小时")
│   ├── SessionsCount ("完成 6 次")
│   └── TreesCount ("种下 4 棵树")
├── CumulativeResultsCard
│   ├── TotalDuration ("累计 50 小时")
│   └── TotalTrees ("累计 50 棵树")
└── LearningRecordsTimeline
    └── DateGroup[]
        └── RecordItem[]
            ├── Title
            ├── SubjectBadge
            ├── DurationDisplay ("45 分钟")
            └── TimeDisplay
```

### 6.3 共享组件清单

| 组件 | 用途 | 关键属性 |
|---|---|---|
| `Button` | 主/次按钮 | `variant: primary \| secondary`, `size`, `disabled`, `loading` |
| `Card` | 卡片容器 | `padding`, `onClick`（可选） |
| `Modal` | 弹窗 | `isOpen`, `onClose`, `title`, 焦点陷阱 |
| `ConfirmDialog` | 二次确认弹窗 | `message`, `onConfirm`, `onCancel`, `destructive` |
| `EmptyState` | 空数据引导 | `icon`, `message`, `actionLabel`, `onAction` |
| `ErrorState` | 错误状态 | `message`, `onRetry` |
| `LoadingState` | 加载状态 | `message`（可选）, `spinner` |
| `SubjectBadge` | 科目标签 | `subject`, `subSubject`（可选） |
| `DurationSelector` | 时长选择器 | `value`, `onChange`, 快捷按钮 + 微调 |
| `PresetCardSelector` | 预设卡片选择 | `presets[]`, `selectedId`, `onSelect`, `groupBy` |
| `RingCountdown` | 环形倒计时 | `totalSeconds`, `remainingSeconds`, `mode: focus\|short_break\|long_break` |
| `ProgressBar` | 进度条 | `value`, `max`, `color`, `animated` |
| `Toast` | 通知提示 | `type: success\|error\|info`, `message`, 自动消失 |
| `SkipLink` | 跳至主内容 | 不可见，聚焦时显示 |
| `ThemeToggle` | 主题切换 | 水波扩散动效 |

### 6.4 状态管理策略

不引入全局状态管理库（Redux、Zustand 等）。使用以下策略：

- **服务端数据**：自定义 `useApi` hook 封装请求状态 `{data, loading, error, refetch}`
- **专注会话**：`useFocusSession` hook 管理当前会话生命周期，通过 `/api/v1/focus/active` 轮询或按需刷新
- **主题**：`useTheme` hook，读写 `localStorage`，切换 CSS class
- **表单**：受控组件 + 本地 state
- **跨页面状态**：通过 URL 参数或共享 hook（如当前选中的日期）

---

## 7. 核心流程设计

### 7.1 专注会话生命周期

```
┌─────────┐   选择预设    ┌─────────┐   点击"开始专注"   ┌─────────────┐
│  空闲态  │ ──────────→ │ 已就绪态  │ ───────────────→ │  专注运行中   │
└─────────┘              └─────────┘                    └──────┬──────┘
     ↑                                                        │
     │                                          ┌─────────────┼─────────────┐
     │                                          ▼             ▼             ▼
     │                                   ┌──────────┐  ┌──────────┐  ┌──────────┐
     │                                   │ 自然结束  │  │ 提前完成  │  │ 取消专注  │
     │                                   └────┬─────┘  └────┬─────┘  └────┬─────┘
     │                                        │             │             │
     │                                        ▼             ▼             ▼
     │                                   ┌──────────┐  ┌──────────┐  ┌──────────┐
     │                                   │ 生成      │  │ 生成      │  │ 不生成    │
     │                                   │studyRecord│  │studyRecord│  │ 记录      │
     │                                   └────┬─────┘  └────┬─────┘  └────┬─────┘
     │                                        │             │             │
     │                                        ▼             ▼             │
     │                                   ┌──────────────────────────┐    │
     │                                   │  专注完成操作面板        │    │
     │                                   │  [继续专注] [短休息]     │◄───┘
     │                                   │  [不休息]                │
     │                                   └──────────┬───────────────┘
     │                                              │
     │                    ┌─────────────────────────┼──────────────┐
     │                    ▼                         ▼              ▼
     │              ┌──────────┐            ┌──────────┐   ┌──────────┐
     │              │ 继续专注  │            │ 短休息    │   │ 不休息    │
     │              │ (→ 就绪态)│            │ (5 分钟)  │   │ (→ 空闲态)│
     │              └──────────┘            └────┬─────┘   └──────────┘
     │                                           │
     │                                           ▼
     │                                    ┌─────────────┐
     │                                    │  休息运行中   │──→ 完成后 → 空闲态
     │                                    └─────────────┘
     │
     └── 第 4 轮完成后显示"一键开始长休息(15分钟)"
```

### 7.2 页面刷新会话恢复

```
页面加载
    │
    ▼
GET /api/v1/focus/active
    │
    ├── 200 + null ──────→ 无活跃会话，正常显示初始态
    │
    ├── 200 + session ───→ 有活跃会话
    │       │
    │       ├── plannedEndAt > now ──→ 恢复倒计时 UI
    │       │   前端根据 startedAt + plannedDurationSeconds 计算剩余秒数
    │       │   启动本地 setInterval 更新（仅用于显示，不替代服务端状态）
    │       │
    │       └── plannedEndAt ≤ now ──→ 服务端自动标记 completed
    │           生成 studyRecord，前端收到 null，正常显示
    │
    └── 错误 ────────────→ 显示错误状态，重试按钮
```

### 7.3 网课导入流程

```
用户粘贴文本
    │
    ▼
POST /api/v1/courses/parse
    │
    ├── 解析结果 ──→ 预览表格展示
    │   ├── 识别到的集数 (标题 + 时长)
    │   ├── 未识别行号 (高亮警告)
    │   ├── 总集数 / 总时长
    │   └── 用户确认
    │       │
    │       ├── 确认 → POST /api/v1/courses → 写入数据库 → 刷新列表
    │       └── 取消 → 关闭弹窗，不写入
    │
    └── 全部无法识别 → 显示错误 + 示例格式提示
```

### 7.4 次日未完成任务处理

```
打开计划页（今天）
    │
    ▼
GET /api/v1/tasks/unfinished?from=昨天日期
    │
    ├── 空数组 ──→ 正常显示今天页面
    │
    └── 有未完成任务 ──→ 弹出 UnfinishedTaskModal
        │
        └── 对每项任务逐一操作：
            ├── 顺延 → 创建新任务（复制内容、科目），日期=今天
            ├── 放弃 → 不操作（任务保留在昨天）
            └── 标记完成 → 更新昨天任务状态=已完成

注意：弹窗可关闭，关闭后未处理项视为放弃。
     弹窗今天只显示一次（localStorage 记录已处理日期）。
```

### 7.5 学习森林种树计算

```
给定时间范围 [start, end)

1. 查询 study_records WHERE created_at IN [start, end)
   AND source = 'focus_session'                       -- 排除 course_video 来源
   AND focus_session_id IS NOT NULL                   -- 来自番茄钟
   AND (course_episode_id IS NULL                     -- 未关联网课
        OR course_episode_id IS NOT NULL)             -- 或关联了网课（只计专注时长）

2. 按科目分组累计 actual_duration_seconds

3. 每个科目树木数量 = floor(total_seconds / 3600)   -- 每满 1 小时 = 1 棵树

4. 距离下一棵树剩余 = 3600 - (total_seconds % 3600)

5. 累计数据：查询所有时间范围的 study_records (相同去重口径)
```

### 7.6 防重复提交机制

- **专注完成**：`POST /focus/:id/complete` 使用数据库事务 + 乐观锁：
  ```sql
  UPDATE focus_sessions
  SET status = 'completed', actual_duration_seconds = ?, completed_at = NOW()
  WHERE id = ? AND status = 'in_progress'
  ```
  若 affected rows = 0，返回 409 Conflict（已处理或不存在）

- **网课集数切换**：`PATCH /courses/:id/episodes/:eid/toggle` 同样使用状态检查

---

## 8. 视觉与主题系统

### 8.1 设计 Token（CSS 自定义属性）

```css
:root {
  /* === 浅色主题（默认）=== */
  --color-bg-page: #F7F4EF;           /* 暖米白 */
  --color-bg-card: #FEFCF9;           /* 暖白卡片表面 */
  --color-text-primary: #2C3E50;      /* 深蓝灰正文 */
  --color-text-secondary: #5D6D7E;    /* 次要文字 */
  --color-accent-primary: #C0392B;    /* 朱砂红主操作 */
  --color-accent-primary-hover: #A93226;
  --color-accent-success: #1E8449;    /* 松绿完成态 */
  --color-accent-deep-green: #0E6655; /* 深青绿长休 */
  --color-border: rgba(0,0,0,0.08);   /* 低对比暖色边界 */
  --color-shadow: rgba(0,0,0,0.06);   /* 柔和阴影 */

  /* 间距 */
  --space-unit: 4px;
  --space-card-padding: 24px;
  --space-section-gap: 32px;

  /* 圆角 */
  --radius-card: 20px;
  --radius-button: 12px;
  --radius-nav: 24px;

  /* 字体 */
  --font-heading: 'Noto Serif SC', 'Songti SC', serif;
  --font-body: 'Noto Sans SC', 'PingFang SC', sans-serif;
  --font-mono: 'JetBrains Mono', 'SF Mono', monospace;  /* 番茄钟、统计数字 */

  /* 动效 */
  --transition-fast: 180ms ease-out;
  --transition-normal: 320ms ease-out;
}

/* === 深色主题 === */
[data-theme="dark"] {
  --color-bg-page: #1A2332;           /* 深墨蓝 */
  --color-bg-card: #212D3D;
  --color-text-primary: #E8E0D5;      /* 暖米白文字 */
  --color-text-secondary: #A0ACB8;
  --color-accent-primary: #E74C3C;    /* 调整对比度的朱砂红 */
  --color-accent-primary-hover: #EC7063;
  --color-accent-success: #27AE60;
  --color-accent-deep-green: #148F77;
  --color-border: rgba(255,255,255,0.08);
  --color-shadow: rgba(0,0,0,0.20);
}
```

### 8.2 主题切换动效

```typescript
// 支持 View Transitions API 时：圆形 clip-path 扩散
// 不支持时：CSS transition 颜色淡变 (~180ms)
// prefers-reduced-motion：直接切换，无动效

function toggleTheme() {
  const newTheme = currentTheme === 'light' ? 'dark' : 'light';
  if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    applyTheme(newTheme);
    return;
  }
  if (document.startViewTransition) {
    const x = /* 按钮中心 x */;
    const y = /* 按钮中心 y */;
    document.startViewTransition(() => applyTheme(newTheme));
    // CSS 中配置 ::view-transition-old/::view-transition-new 使用 clip-path
  } else {
    applyTheme(newTheme); // CSS transition 处理颜色过渡
  }
}
```

### 8.3 响应式断点

| 断点 | 布局 |
|---|---|
| ≥ 1200px | 桌面：12 栏网格，主次栏 |
| 768px–1199px | 平板：8 栏简化网格 |
| < 768px | 移动：单列，自然纵向滚动 |

首页在 `1366×768` 桌面视口中不产生纵向滚动（包括导航）。

---

## 9. 可访问性设计

### 9.1 检查清单映射

| 需求条目 | 实现方式 |
|---|---|
| WCAG AA 文字对比度 | 所有文字/背景组合 ≥ 4.5:1（正文）、≥ 3:1（大文本 18px+） |
| 状态不得仅用颜色 | `SubjectBadge` 使用图标+颜色+文字；`TaskItem` 完成态使用删除线+圆点+颜色 |
| 44px 触控目标 | 所有交互元素 `min-height: 44px; min-width: 44px` |
| 可见键盘焦点 | `:focus-visible` outline 2px + 2px offset，使用主色 |
| Tab 顺序 | DOM 顺序即交互顺序，Modal 打开时焦点陷阱 |
| 跳至主内容 | `<SkipLink>` 作为页面第一个可聚焦元素 |
| aria-label | 所有图标按钮设置 `aria-label` |
| 表单 label | 所有 input 关联 `<label>` 或 `aria-labelledby` |
| 字段错误 | 错误信息在字段附近，使用 `aria-describedby` 关联 |
| 反馈状态 | Button 支持 `loading`/`disabled`；Toast 通知；ErrorState 重试 |
| prefers-reduced-motion | 禁用非必要动效，环形计时器取消平滑过渡但保留数值更新 |
| timer role | `RingCountdown` 设置 `role="timer"` + `aria-label="剩余时间 X 分 Y 秒"` |

### 9.2 拖拽排序的键盘替代

```
拖拽手柄 (aria-grabbed) + 键盘操作:
- Tab: 聚焦到排序手柄
- Space: 进入排序模式
- ArrowUp/ArrowDown: 移动项目
- Space: 确认新位置（退出排序模式）
- Escape: 取消排序

同时提供"上移"/"下移"隐藏按钮（仅屏幕阅读器可感知）
```

---

## 10. 测试策略

### 10.1 测试层次

```
┌─────────────────────────────────┐
│  E2E (Playwright)               │  ← 首页 + 统计页冒烟，不依赖真实数据库
├─────────────────────────────────┤
│  集成测试 (Vitest + TEST_DB)    │  ← API 路由 + 数据库交互
├─────────────────────────────────┤
│  单元测试 (Vitest)              │  ← 工具函数、Zod schema、时长计算
└─────────────────────────────────┘
```

### 10.2 测试覆盖要求

| 模块 | 测试要点 | 类型 |
|---|---|---|
| 学习预设 | 锁定入口校验、5–120 分钟限制、5 分钟倍数、快捷按钮/微调 | 单元 + 集成 |
| 番茄钟 | 预设必选、时长带入、休息固定时长、刷新恢复、防重复、第 4 轮提示 | 单元 + 集成 |
| 网课 | 7 分区展示、分区锁定、预览不写入、删除确认、快照保留 | 集成 |
| 统计 | 日/周/月切换、未来拒绝、种树计算、去重口径、累计 | 集成 |
| 前后端 Schema | Zod schema 一致性、边界值 | 单元 |
| 首页 | 打开、倒计时、1366×768 无滚动 | E2E |
| 统计页 | 打开、模式切换、学习记录展示 | E2E |

### 10.3 测试配置要点

- **数据库集成测试**：创建独立数据库 `kaoyandaily_test`，测试前建表、测试后清理（或每个测试套件使用独立数据库）
- **E2E**：使用 mock API（MSW 或 Playwright route interception），不依赖真实数据库
- **CI 模拟**：`npm test && npm run test:e2e && npm run lint && npm run build`

---

## 11. 实施计划

### 阶段 0：项目脚手架（预计 0.5 天）

- [ ] 初始化 `package.json`、`tsconfig.json`（root + client + server）
- [ ] Vite + React 项目初始化（`client/`）
- [ ] Express 项目初始化（`server/`）
- [ ] `shared/` 目录结构 + Zod 安装
- [ ] `.env.example` + `.gitignore`
- [ ] ESLint 配置
- [ ] Vitest 配置
- [ ] Playwright 配置
- [ ] 数据库建表 SQL 编写并在本地 MySQL 执行

### 阶段 1：基础框架（预计 1 天）

- [ ] 数据库连接池 + 健康检查（`server/src/db/connection.ts`）
- [ ] Express 中间件：CORS、错误处理、Zod 校验包装器
- [ ] 共享 Schema 完整编写（`shared/src/schemas/*`）
- [ ] 前端主题系统（CSS 自定义属性、浅色/深色、主题切换）
- [ ] 前端全局布局：`TopNav`（含所有 7 个导航项）、`SkipLink`、页面壳
- [ ] 前端路由（简单 hash 路由）
- [ ] 前端共享组件：`Button`、`Card`、`Modal`、`EmptyState`、`ErrorState`、`LoadingState`、`Toast`

### 阶段 2：学习预设（预计 1 天）

- [ ] API：CRUD 预设
- [ ] 前端：`PresetsPage` — 分组卡片展示
- [ ] 前端：`PresetForm`（创建/编辑弹窗）— 带科目锁定逻辑
- [ ] 前端：`DurationSelector` — 25/45/60 快捷 + -5/+5 微调
- [ ] 测试：预设 CRUD、锁定入口、时长校验

### 阶段 3：番茄钟（预计 1.5 天）

- [ ] API：start / complete / cancel / active / start-break
- [ ] 前端：`PomodoroPage` — 预设选择 → 时长调整 → 开始专注
- [ ] 前端：`RingCountdown` — SVG 环形倒计时，timer 无障碍语义
- [ ] 前端：`useFocusSession` hook — 会话生命周期 + 刷新恢复
- [ ] 前端：Post-focus 操作面板（继续/短休/不休息/长休提示）
- [ ] 防重复提交机制
- [ ] 测试：完整番茄钟流程

### 阶段 4：每日任务 & 复盘（预计 1.5 天）

- [ ] API：任务 CRUD + 排序 + 未完成任务查询
- [ ] API：复盘 upsert
- [ ] 前端：`PlanPage` — 今天/明天页签
- [ ] 前端：`TaskList` — 重要/普通分组，可排序
- [ ] 前端：`TaskItem` — 原地编辑、完成切换、图钉、删除确认
- [ ] 前端：键盘可操作排序替代方案
- [ ] 前端：`UnfinishedTaskModal` — 次日未完成任务处理
- [ ] 前端：`DailyReviewSection` — 自由文本 + 保存状态
- [ ] 测试：任务 CRUD、排序、未完成任务处理

### 阶段 5：首页（预计 0.5 天）

- [ ] 前端：`HomePage` — 倒计时、今日专注、任务摘要、预设概览
- [ ] 1366×768 视口无纵向滚动
- [ ] 空状态引导
- [ ] 测试：首页冒烟

### 阶段 6：网课管理（预计 1.5 天）

- [ ] API：课程 CRUD + 解析导入 + 集数切换
- [ ] 前端：`CoursesPage` — 7 个固定分区
- [ ] 前端：`ImportModal` — 粘贴→解析预览→确认
- [ ] 前端：`CourseDetailPage` — 集数列表 + 进度条
- [ ] 前端：删除确认弹窗 + 错误重试
- [ ] 前端：集数完成/取消 + 进度更新
- [ ] 后端：删除后快照保留逻辑
- [ ] 测试：导入、删除、快照保留

### 阶段 7：统计 — 学习森林（预计 1.5 天）

- [ ] API：`GET /api/v1/statistics/forest` — 去重统计算法
- [ ] 前端：`StatisticsPage` — 日/周/月切换 + 范围导航
- [ ] 前端：`ForestVisualization` — 三科树种展示
- [ ] 前端：`PeriodResultsCard` + `CumulativeResultsCard`
- [ ] 前端：`LearningRecordsTimeline` — 按日期分组
- [ ] 空数据 + 未来范围拒绝
- [ ] 测试：完整统计流程

### 阶段 8：测试完善 & 验收（预计 1 天）

- [ ] 补充所有单元/集成测试
- [ ] Playwright 冒烟测试
- [ ] 运行 `npm test`、`npm run test:e2e`、`npm run lint`、`npm run build`
- [ ] 修复所有失败
- [ ] `git diff --check` 空白检查
- [ ] 响应式验证（桌面 1366×768 + 移动端）
- [ ] 深色主题独立验证
- [ ] 键盘可访问性验证
- [ ] WCAG AA 对比度检查

---

## 12. 风险与约束记录

| # | 风险/约束 | 影响 | 缓解措施 |
|---|---|---|---|
| 1 | 需求文档提及"复用现有 focusSession 和 studyRecord 体系"，但当前为全新项目 | 无现有体系可复用，需从零设计 | 本文档已完整设计两个实体，与需求语义一致 |
| 2 | "不新增数据库迁移" — 初始建表后不引入迁移工具 | 未来 schema 变更需手动管理 | 初始 Schema 设计充分考虑扩展性（快照字段、NULL FK） |
| 3 | 无账号系统 — 单用户本地使用 | 无权限校验需求，简化后端 | 无冲突，但需确保 API 不做用户隔离 |
| 4 | 考试日期硬编码 2026-12-20 | 倒计时计算不可配置 | 使用单一常量，如需变更通过环境变量 |
| 5 | 课程补录数据不计入森林统计 | `source='course_video'` 记录需区分处理 | 统计查询明确过滤 `source` 字段 |
| 6 | 删除课程保留统计数据 | `study_records` 快照脱离原实体 | 所有记录在写入时复制快照字段，不依赖 JOIN |
| 7 | 前端改进建议需独立输出 | 不可混入功能代码 | 阶段 8 后单独产出 |
| 8 | 仅支持今天/明天的任务管理 | 不支持查看更远日期 | UI 日期选择器仅显示两个选项 |

---

## 附录 A：npm scripts 设计

```json
{
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "cd client && vite",
    "dev:server": "cd server && tsx watch src/index.ts",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && vite build",
    "build:server": "cd server && tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "lint": "eslint . --ext .ts,.tsx",
    "db:init": "cd server && tsx src/db/init.ts"
  }
}
```

## 附录 B：.env.example 模板

```bash
# 数据库
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=kaoyandaily

# 测试数据库
TEST_DB_NAME=kaoyandaily_test

# 服务端口
SERVER_PORT=3001
```

## 附录 C：关键常量汇总

| 常量 | 值 | 位置 |
|---|---|---|
| 考试日期 | 2026-12-20 | 共享常量 / 环境变量 |
| 预设时长范围 | 5–120 分钟，5 分钟倍数 | `shared/schemas/common.ts` |
| 快捷时长按钮 | 25, 45, 60 分钟 | `shared/schemas/common.ts` |
| 短休时长 | 5 分钟 | `shared/schemas/common.ts` |
| 长休时长 | 15 分钟 | `shared/schemas/common.ts` |
| 长休触发轮次 | 第 4 轮 | `shared/schemas/common.ts` |
| 种树阈值 | 每 1 小时 = 3600 秒 | 统计 SQL 查询 |
| 网课固定分区数 | 7 | 常量数组 |
| API 根路径 | `/api/v1` | Express 路由前缀 |
| 首页视口约束 | 1366×768 无纵向滚动 | 前端响应式检查 |
